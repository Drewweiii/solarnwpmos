"""The report's prose and equations.

Kept as data rather than one giant HTML file so the maths, the figures and the
screenshots can be checked against the code that produced them.

Language: Thai body text with English technical terms retained, which is the
convention for Thai engineering reporting - translating "least squares" or
"pinball loss" would make the report harder to check against the literature,
not easier.

EVERY equation here is one the system actually evaluates. Where a figure is
quoted it was computed by figures.py, not typed in. Where something is an
assumption rather than a measurement, the text says so - the project's standing
rule against presenting estimates as measurements applies to its own report
first of all.
"""

from __future__ import annotations

# --------------------------------------------------------------------------
# Site constants, quoted throughout. Sourced from config/assets.yaml and the
# project's own documentation.
# --------------------------------------------------------------------------
SITE = {
    "name": "PTT LNG Terminal 2 (หนองแฟบ)",
    "province": "ระยอง",
    "lat": 12.71,
    "lon": 101.15,
    "capacity_kwp": 429.0,
    "modules": 600,
    "annual_kwh": 850_109,
    "tariff": 2.5368,
    "zones": "GIS, ISB, Jetty",
}

CH = []


def chapter(num: str, title: str, body: str) -> None:
    CH.append({"num": num, "title": title, "body": body})


# ===========================================================================
chapter("1", "บทนำและขอบเขตของงาน", r"""
<p><b>โครงการ</b> ระบบพยากรณ์และจัดการพลังงานแสงอาทิตย์ (Solar Forecasting &amp;
Energy Management System) สำหรับโรงไฟฟ้าพลังงานแสงอาทิตย์บนหลังคาและพื้นดินของ
คลังก๊าซธรรมชาติเหลว <b>PTT LNG Terminal 2 (หนองแฟบ)</b> จังหวัดระยอง
ที่พิกัด <span class="num">12.71°N, 101.15°E</span></p>

<table class="spec">
<tr><th>กำลังติดตั้งรวม (DC)</th><td class="num">429.0 kWp</td></tr>
<tr><th>จำนวนแผง</th><td class="num">600 แผง</td></tr>
<tr><th>โซนติดตั้ง</th><td>GIS · ISB · Jetty</td></tr>
<tr><th>พลังงานที่ผลิตได้ต่อปี (ประเมิน)</th><td class="num">850,109 kWh/ปี</td></tr>
<tr><th>อัตราค่าไฟที่ใช้อ้างอิง</th><td class="num">2.5368 บาท/kWh</td></tr>
</table>

<h3>1.1 คำถามที่ระบบนี้ตอบ</h3>
<p>ระบบตอบคำถามสี่ข้อที่แยกกันชัดเจน และแต่ละข้อใช้คณิตศาสตร์คนละชุด:</p>
<ol>
<li><b>พรุ่งนี้จะผลิตไฟได้เท่าไร</b> — day-ahead forecast ล่วงหน้าถึง 72 ชั่วโมง</li>
<li><b>อีก 1–6 ชั่วโมงข้างหน้าจะผลิตได้เท่าไร</b> — intra-day forecast</li>
<li><b>คำพยากรณ์ที่ผ่านมาแม่นแค่ไหน</b> — verification ด้วยข้อมูลที่เผยแพร่ไปแล้วจริง</li>
<li><b>การลงทุนนี้คุ้มหรือไม่</b> — NPV / IRR / LCOE / payback</li>
</ol>

<div class="callout warn">
<p><b>ข้อจำกัดที่สำคัญที่สุดของงานนี้ และต้องระบุตั้งแต่ต้น</b></p>
<p><b>ไซต์นี้ไม่มีมิเตอร์วัดกำลังผลิตจริง</b> (no generation meter) ตัวเลขที่เว็บเรียกว่า
&ldquo;ค่าจริง&rdquo; ทุกจุด คือผลลัพธ์ของแบบจำลองฟิสิกส์ที่ป้อนด้วย<b>สภาพอากาศที่เกิดขึ้นจริง</b>
ไม่ใช่ค่าที่อ่านจากมิเตอร์</p>
<p>ผลที่ตามมาทางวิชาการ: ค่า error ทุกค่าในบทที่ 9 วัด<b>ความคลาดเคลื่อนของการพยากรณ์อากาศ
ที่ส่งผ่านแบบจำลองฟิสิกส์</b> ไม่ใช่ความคลาดเคลื่อนเทียบกับการผลิตจริง ซึ่งเป็นคนละปริมาณกัน
และรายงานนี้จะไม่อ้างเกินกว่านั้น</p>
</div>

<h3>1.2 เหตุผลเชิงระบบ: ทำไมไซต์นี้จึงเน้นการลงทุนมากกว่าการสั่งการ</h3>
<p>ไซต์นี้<b>เชื่อมกับกริดเต็มรูปแบบ ไม่มีแบตเตอรี่ และกำลังติดตั้งถูกจำกัดด้วยพื้นที่</b>
ในเชิงปฏิบัติการจึงไม่มี &ldquo;การตัดสินใจ&rdquo; ที่การพยากรณ์รายชั่วโมงจะไปเปลี่ยนได้
(ไม่มีการสั่งชาร์จ/คายแบต ไม่มีการ curtail) การพยากรณ์จึงมีค่าในเชิง<b>ความเข้าใจและการตรวจสอบ</b>
ส่วนคำถามที่มีผลต่อการตัดสินใจจริงคือ<b>ความคุ้มค่าการลงทุน</b> ซึ่งเป็นที่มาของบทที่ 10</p>
""")

# ===========================================================================
chapter("2", "สถาปัตยกรรมระบบและการไหลของข้อมูล", r"""
<p>ระบบแบ่งเป็นชั้นตามหน้าที่ แต่ละชั้นเป็น Python package แยกกัน และทดสอบแยกกันได้</p>

<pre class="flow">
  แหล่งข้อมูลภายนอก          ชั้นประมวลผล                ชั้นเผยแพร่
  ─────────────────         ──────────────              ───────────
  GFS (NOAA)      ─┐
  Himawari-9      ─┤        ingestion/*  ─→  features/*  ─→  forecast/*  ─┐
  NASA POWER      ─┤             │              │               │        │
  PVGIS           ─┤             │              │               ↓        ↓
  CAMS / TMD      ─┘             ↓              ↓          simulation/  api/
                            local_store    clearsky           │          │
                            (SQLite)       poa, shading       ↓          ↓
                                           panel_geometry  financial/   web/
</pre>

<table class="spec">
<tr><th>libs/nongfab_common</th><td>ทะเบียนสินทรัพย์ (assets.yaml), ชนิดข้อมูลร่วม</td></tr>
<tr><th>ingestion/*</th><td>ดึงข้อมูลจากแหล่งภายนอก 5 แหล่ง</td></tr>
<tr><th>features/*</th><td>ดาราศาสตร์ดวงอาทิตย์, clear-sky, POA, เงา, เรขาคณิตแผง</td></tr>
<tr><th>forecast/*</th><td>โมเดลพยากรณ์ 4 ตระกูล + verification</td></tr>
<tr><th>simulation/*</th><td>แบบจำลองการสูญเสีย, TOU, การหามุมที่ดีที่สุด</td></tr>
<tr><th>financial/*</th><td>NPV / IRR / LCOE / payback / Monte Carlo</td></tr>
<tr><th>api/ + web/</th><td>FastAPI + React (TypeScript)</td></tr>
</table>

<h3>2.1 หลักการออกแบบที่ควบคุมทั้งระบบ</h3>
<p>ทุกค่าที่ปรับได้ในระบบ (92 ค่า ใน 10 กลุ่ม) มีเมทาดาทา <span class="code">origin</span>
ติดอยู่ ซึ่งบอกว่าค่านั้น<b>มาจากไหน</b>:</p>
<table class="spec">
<tr><th><span class="code">confirmed</span></th><td>ผู้ใช้ยืนยันแล้ว เป็นค่าจริงของไซต์นี้</td></tr>
<tr><th><span class="code">as-built</span></th><td>จากเอกสาร as-built / การรังวัดจริง</td></tr>
<tr><th><span class="code">literature</span></th><td>ค่าอ้างอิงจากงานวิจัย ไม่ได้วัดที่ไซต์นี้</td></tr>
<tr><th><span class="code">placeholder</span></th><td>ค่าประมาณ ยังไม่ยืนยัน</td></tr>
<tr><th><span class="code">derived</span></th><td>คำนวณจากค่าอื่นในระบบ</td></tr>
</table>
<p>เมทาดาทานี้ไม่ได้มีไว้ประดับ — ระบบมี endpoint <span class="code">/provenance</span>
ที่ตามรอยตัวเลขที่เผยแพร่กลับไปยังต้นทาง และ<b>รายงานความน่าเชื่อถือด้วย
&ldquo;ขั้นที่อ่อนที่สุดในสาย&rdquo; (weakest link) ไม่ใช่ค่าเฉลี่ย</b> เพราะตัวเลขจะแข็งแรงได้
ไม่เกินข้อมูลที่อ่อนที่สุดที่มันใช้</p>
""")

# ===========================================================================
chapter("3", "พีชคณิตเชิงเส้นของเรขาคณิตแผงและระบบพิกัด", r"""
<p>ก่อนคำนวณพลังงานได้ ต้องรู้ก่อนว่าแผงแต่ละแผง<b>อยู่ที่ไหนและหันไปทางใด</b>
ส่วนนี้เป็นพีชคณิตเชิงเส้นล้วน</p>

<h3>3.1 การแปลงพิกัดภูมิศาสตร์เป็นระบบพิกัดท้องถิ่น (ENU)</h3>
<p>พิกัดที่รังวัดมาเป็น latitude/longitude แต่การคำนวณเงาและเรขาคณิตต้องทำในระบบเมตร
จึงแปลงเป็นระบบ <b>East-North-Up (ENU)</b> โดยใช้การประมาณระนาบสัมผัส (tangent-plane)
ซึ่งใช้ได้ดีเมื่อพื้นที่มีขนาดไม่กี่ร้อยเมตร:</p>
$$
\begin{bmatrix} E \\ N \end{bmatrix}
=
\begin{bmatrix} R\cos\varphi_0 \cdot (\lambda - \lambda_0) \\ R\,(\varphi - \varphi_0) \end{bmatrix}
$$
<p>โดย \(R\) คือรัศมีโลก \(\varphi_0, \lambda_0\) คือจุดอ้างอิงของไซต์
ตัวประกอบ \(\cos\varphi_0\) คือสิ่งที่ทำให้ระยะทางตามลองจิจูดหดลงตามละติจูด —
ที่ \(\varphi_0 = 12.71°\) ได้ \(\cos\varphi_0 \approx 0.9755\)</p>

<h3>3.2 เมทริกซ์การหมุน: จากพิกัดของแถวแผงสู่พิกัดไซต์</h3>
<p>แผงถูกวางเป็นตารางตามแนวแถว จึงสร้างพิกัดในระบบของแถวก่อน \((u, v)\)
แล้วหมุนเข้าสู่ระบบ ENU ด้วยมุม azimuth \(\gamma\) ของอาร์เรย์
โค้ดจริง (<span class="code">panel_geometry._rotate_to_local_en</span>) คือ:</p>
$$
\begin{bmatrix} E \\ N \end{bmatrix}
=
\underbrace{\begin{bmatrix} \cos\gamma & -\sin\gamma \\ -\sin\gamma & -\cos\gamma \end{bmatrix}}_{\mathbf{R}(\gamma)}
\begin{bmatrix} u \\ v \end{bmatrix}
$$
<p>สังเกตว่า \(\det \mathbf{R} = -\cos^2\gamma - \sin^2\gamma = -1\) — เมทริกซ์นี้เป็น
<b>reflection ไม่ใช่ rotation บริสุทธิ์</b> เพราะแกน \(v\) ถูกกลับทิศเพื่อให้
&ldquo;หน้าแผง&rdquo; ชี้ไปทาง azimuth ที่กำหนด ซึ่งเป็นการเลือก convention ไม่ใช่ความผิดพลาด</p>

<h3>3.3 เวกเตอร์ปกติของแผงและมุมตกกระทบ</h3>
<p>แผงที่เอียง \(\beta\) และหันไปทาง \(\gamma\) มีเวกเตอร์ปกติหนึ่งหน่วย:</p>
$$
\hat{\mathbf{n}} = \begin{bmatrix} \sin\beta\sin\gamma \\ \sin\beta\cos\gamma \\ \cos\beta \end{bmatrix}
$$
<p>และทิศของดวงอาทิตย์ที่มุมเงย \(\alpha\) มุมทิศ \(A\):</p>
$$
\hat{\mathbf{s}} = \begin{bmatrix} \cos\alpha\sin A \\ \cos\alpha\cos A \\ \sin\alpha \end{bmatrix}
$$
<p><b>มุมตกกระทบ</b> \(\theta\) คือ dot product ของสองเวกเตอร์นี้ — เป็นหัวใจของการคำนวณ
พลังงานทั้งหมด เพราะกำลังที่แผงรับได้แปรตาม \(\cos\theta\):</p>
$$
\cos\theta = \hat{\mathbf{n}} \cdot \hat{\mathbf{s}}
= \sin\beta\cos\alpha\cos(A - \gamma) + \cos\beta\sin\alpha
$$
<p>เมื่อ \(\cos\theta \le 0\) แสดงว่าแสงมาจากด้านหลังแผง และรังสีตรงเป็นศูนย์</p>
""")

# ===========================================================================
chapter("4", "ดาราศาสตร์ดวงอาทิตย์และแบบจำลองรังสี", r"""
<h3>4.1 ตำแหน่งดวงอาทิตย์</h3>
<p><b>Declination</b> (มุมเอียงของดวงอาทิตย์เทียบเส้นศูนย์สูตร) ประมาณด้วยสูตร Cooper:</p>
$$ \delta = 23.45° \sin\!\left(\frac{360(284 + n)}{365}\right) $$
<p>โดย \(n\) คือลำดับวันในปี</p>

<figure><img src="figures/01-declination.svg" alt="declination">
<figcaption><b>รูปที่ 1</b> Declination ตลอดปี เส้นประม่วงคือละติจูดของไซต์ (12.71°N)
จุดที่เส้นทั้งสองตัดกันคือวันที่ดวงอาทิตย์ผ่านจุดเหนือศีรษะพอดี ซึ่ง
<b>เกิดขึ้นปีละสองครั้ง</b> เพราะ \(|\varphi| &lt; 23.45°\) — เป็นลักษณะเฉพาะของไซต์เขตร้อน
และเป็นเหตุผลที่เส้นโค้งพลังงานรายปีของไซต์นี้มีสองยอด ไม่ใช่ยอดเดียว</figcaption></figure>

<p><b>มุมเงย</b> \(\alpha\) จากตรีโกณมิติทรงกลม โดย \(\omega\) คือ hour angle (15° ต่อชั่วโมง):</p>
$$ \sin\alpha = \sin\varphi\sin\delta + \cos\varphi\cos\delta\cos\omega $$

<figure><img src="figures/02-sun-paths.svg" alt="sun path" class="narrow">
<figcaption><b>รูปที่ 2</b> แผนภาพเส้นทางดวงอาทิตย์แบบ polar ที่หนองแฟบ
จุดศูนย์กลางคือเหนือศีรษะ ขอบวงคือขอบฟ้า<br>
<b>ข้อสังเกตสำคัญ:</b> เส้นเดือนมิถุนายน (ส้ม) โค้งผ่าน<b>ทางทิศเหนือ</b> เพราะ
\(\delta = 23.4° &gt; \varphi = 12.7°\) ทำให้ดวงอาทิตย์ขึ้นเลยจุดเหนือศีรษะไปทางเหนือ
ขณะที่เดือนธันวาคม (ม่วง) โค้งผ่านทางทิศใต้ ปรากฏการณ์นี้เป็นเหตุผลที่แผนภาพต้องเป็น
polar — บนแกนเชิงเส้น เส้นเดือนมิถุนายนจะข้ามรอยต่อ 0°/360° และถูกวาดผิด</figcaption></figure>

<h3>4.2 Clearness index และการแยกองค์ประกอบรังสี (Erbs)</h3>
<p>แหล่งข้อมูลทุกแหล่งให้ <b>GHI</b> (รังสีรวมบนระนาบแนวนอน) แต่รังสีตรง (beam) กับ
รังสีกระจาย (diffuse) ตกกระทบแผงเอียงต่างกันมาก จึงต้องแยกก่อน
ตัวแปรที่ใช้แยกคือ <b>clearness index</b>:</p>
$$ k_t = \frac{GHI}{I_0 \cos\theta_z} $$
<p>โดย \(I_0\) คือค่าคงที่สุริยะนอกบรรยากาศ (\(\approx 1361\) W/m²) และ \(\theta_z\)
คือมุมเซนิท ค่า \(k_t\) จึงเป็นสัดส่วนของรังสีที่ทะลุบรรยากาศลงมาได้</p>
<p>ความสัมพันธ์ Erbs (1982) ให้สัดส่วนรังสีกระจายเป็นฟังก์ชันแบบแบ่งช่วง:</p>
$$
\frac{DHI}{GHI} =
\begin{cases}
1 - 0.09k_t & k_t \le 0.22 \\[4pt]
\begin{aligned}0.9511 &- 0.1604k_t + 4.388k_t^2 \\ &- 16.638k_t^3 + 12.336k_t^4\end{aligned} & 0.22 < k_t \le 0.80 \\[4pt]
0.165 & k_t > 0.80
\end{cases}
$$

<figure><img src="figures/03-erbs.svg" alt="erbs">
<figcaption><b>รูปที่ 3</b> ฟังก์ชัน Erbs ฟ้าปิด (\(k_t\) ต่ำ) แสงเกือบทั้งหมดเป็นรังสีกระจาย
ฟ้าใส (\(k_t\) สูง) เหลือรังสีกระจายเพียง 16.5%<br>
ค่า \(k_t\) เดียวกันนี้ยังถูกใช้ในบทที่ 9 เพื่อ<b>จำแนกสภาพฟ้า</b>แล้วรายงานความแม่นยำแยกตามสภาพ</figcaption></figure>

<h3>4.3 การฉายลงระนาบเอียง (Hay-Davies transposition)</h3>
<p>รังสีบนระนาบแผง (POA) คือผลรวมสามส่วน:</p>
$$ I_{POA} = \underbrace{DNI\cos\theta}_{\text{beam}} + \underbrace{I_d}_{\text{sky diffuse}} + \underbrace{GHI\,\rho\,\frac{1-\cos\beta}{2}}_{\text{ground reflected}} $$
<p>ระบบใช้แบบจำลอง <b>Hay-Davies</b> สำหรับส่วน sky diffuse ซึ่งแยกรังสีกระจายรอบดวงอาทิตย์
(circumsolar) ออกจากส่วนที่กระจายทั่วท้องฟ้า โดยใช้ <b>anisotropy index</b>
\(A_i = DNI / I_0\):</p>
$$ I_d = DHI\left[ A_i \frac{\cos\theta}{\cos\theta_z} + (1 - A_i)\frac{1 + \cos\beta}{2} \right] $$
<p>เหตุผลที่ไม่ใช้แบบจำลอง isotropic ที่ง่ายกว่า: ส่วน circumsolar คือสิ่งที่<b>แยกแยะ
มุมติดตั้งหนึ่งจากอีกมุมหนึ่งได้จริง</b>บริเวณใกล้จุดที่ดีที่สุด ถ้าใช้ isotropic
การหามุมที่ดีที่สุดในบทที่ 7 จะให้คำตอบที่แบนราบเกินจริง</p>
<p>ค่า albedo \(\rho = 0.25\) เป็นค่ามาตรฐานสำหรับพื้นอุตสาหกรรมผสม
<b>ไม่ได้วัดที่ไซต์นี้</b> (origin = literature)</p>
""")

# ===========================================================================
chapter("5", "แบบจำลองเงาแถวต่อแถวเชิงวิเคราะห์", r"""
<p>แผงที่อยู่แถวหลังถูกแถวหน้าบังเงาเมื่อดวงอาทิตย์อยู่ต่ำ ระบบคำนวณด้วย
แบบจำลองภาคตัดขวาง 2 มิติสำหรับแถวขนานที่ซ้ำกัน</p>

<figure><img src="figures/05-shading-geometry.svg" alt="shading geometry">
<figcaption><b>รูปที่ 4</b> ภาคตัดขวางที่ใช้ derive สูตร แผงยาว \(L\) เอียง \(\beta\)
ระยะระหว่างแถว \(p\)</figcaption></figure>

<h3>5.1 การพิสูจน์</h3>
<p>แผงเอียง \(\beta\) ยาวตามแนวลาด \(L\) มีเงาฉายลงพื้นดังนี้:</p>
$$ \text{ฐานแนวนอนของแถว} \quad b = L\cos\beta $$
$$ \text{ความสูงขอบบน} \quad h = L\sin\beta $$
<p>เงาที่ทอดไปด้านหลังมีความยาวแนวนอน โดย \(\alpha\) คือมุมเงยดวงอาทิตย์:</p>
$$ x_{\text{shadow}} = \frac{h}{\tan\alpha}\cos(A - \gamma) $$
<p>เทอม \(\cos(A - \gamma)\) คือส่วนประกอบของทิศทางเงาที่ชี้ไป<b>ด้านหลังแถว</b>
เมื่อดวงอาทิตย์อยู่ตรงหน้าอาร์เรย์พอดี (\(A = \gamma\)) เทอมนี้เป็น 1 และเงายาวที่สุด
เมื่อดวงอาทิตย์เลยไปด้านหลัง (\(|A - \gamma| &gt; 90°\)) เทอมนี้ \(\le 0\)
และในแบบจำลองนี้ถือว่าไม่มีเงาทอดไปข้างหลัง</p>
<p>เงาจะเลยขอบหน้าของแถวถัดไปเป็นระยะ:</p>
$$ x_s = (b - p) + x_{\text{shadow}} $$
<p>ดังนั้น<b>สัดส่วนที่ถูกบัง</b> คือ:</p>
$$ f_{\text{shade}} = \operatorname{clip}\!\left(\frac{x_s}{b},\, 0,\, 1\right)
= \operatorname{clip}\!\left(\frac{(L\cos\beta - p) + \dfrac{L\sin\beta}{\tan\alpha}\cos(A-\gamma)}{L\cos\beta},\, 0,\, 1\right)$$
<p>และ <b>solar access</b> ที่เว็บแสดงคือ \((1 - f_{\text{shade}}) \times 100\%\)</p>

<figure><img src="figures/04-shading-elevation.svg" alt="shading curve">
<figcaption><b>รูปที่ 5</b> สัดส่วนที่ถูกบังเทียบมุมเงยดวงอาทิตย์ ที่ระยะแถวสามค่า
เส้นแดง (pitch 3.0 m) ถูกบังเกือบเต็มเมื่อดวงอาทิตย์ต่ำกว่า ~15°
ขณะที่เส้นเขียว (5.0 m) แทบไม่ถูกบังเลย นี่คือการแลกเปลี่ยนโดยตรงระหว่าง
<b>ความหนาแน่นของการติดตั้ง</b>กับ<b>การสูญเสียจากเงา</b></figcaption></figure>

<div class="callout">
<p><b>ขอบเขตที่แบบจำลองนี้ไม่ครอบคลุม</b> — พิจารณาเฉพาะเงาจากแถวที่อยู่ติดกันเท่านั้น
เมื่อดวงอาทิตย์ต่ำมาก เงาจะยาวพอไปถึงแถวที่ 2 และ 3 ด้วย ซึ่งแบบจำลองนี้ไม่จับ
และไม่มีข้อมูลสิ่งกีดขวางภายนอก (ต้นไม้ อาคาร) ใน assets.yaml
จึงไม่รวมเงาจากสิ่งกีดขวาง ระบุไว้เป็น &ldquo;known gap&rdquo; ไม่ใช่ปั้นข้อมูลขึ้นมาแทน</p>
</div>
""")

# ===========================================================================
chapter("6", "กำลังสองน้อยที่สุดและการถดถอยเชิงเส้น", r"""
<p>บทนี้เป็นแกนพีชคณิตเชิงเส้นของงาน ระบบใช้ least squares สองแห่ง
ด้วยเหตุผลต่างกัน และรูปแบบต่างกัน</p>

<h3>6.1 แบบจำลองแปลงรังสีเป็นกำลังไฟฟ้า</h3>
<p>แบบจำลอง PV conversion เป็นเชิงเส้นในสองตัวแปร — รังสี \(I\) และอุณหภูมิ \(T\):</p>
$$ P = \beta I + \gamma T + c $$
<p>ความหมายเชิงฟิสิกส์: \(\beta\) คือกำลังต่อหนึ่งหน่วยรังสี \(\gamma\) คือการลดทอนตามอุณหภูมิ
(เป็นลบ) และ \(c\) คือจุดตัดแกน</p>

<h4>รูปแบบเมทริกซ์</h4>
<p>สำหรับข้อมูล \(m\) จุด สร้าง <b>design matrix</b> \(\mathbf{A} \in \mathbb{R}^{m\times3}\):</p>
$$
\mathbf{A} = \begin{bmatrix}
I_1 & T_1 & 1 \\
I_2 & T_2 & 1 \\
\vdots & \vdots & \vdots \\
I_m & T_m & 1
\end{bmatrix}, \qquad
\mathbf{x} = \begin{bmatrix}\beta\\\gamma\\c\end{bmatrix}, \qquad
\mathbf{b} = \begin{bmatrix}P_1\\P_2\\\vdots\\P_m\end{bmatrix}
$$
<p>ปัญหาคือหา \(\mathbf{x}\) ที่ทำให้ค่าคลาดเคลื่อนกำลังสองน้อยที่สุด:</p>
$$ \hat{\mathbf{x}} = \arg\min_{\mathbf{x}} \lVert \mathbf{A}\mathbf{x} - \mathbf{b} \rVert_2^2 $$

<h4>สมการปกติ (normal equations) และเหตุผลที่ไม่ใช้มันตรงๆ</h4>
<p>อนุพันธ์เป็นศูนย์ให้:</p>
$$ \mathbf{A}^\mathsf{T}\mathbf{A}\,\hat{\mathbf{x}} = \mathbf{A}^\mathsf{T}\mathbf{b}
\quad\Longrightarrow\quad
\hat{\mathbf{x}} = (\mathbf{A}^\mathsf{T}\mathbf{A})^{-1}\mathbf{A}^\mathsf{T}\mathbf{b} = \mathbf{A}^{+}\mathbf{b} $$
<p>โดย \(\mathbf{A}^{+}\) คือ Moore-Penrose pseudo-inverse</p>
<p><b>แต่โค้ดไม่ได้แก้สมการปกติโดยตรง</b> — ใช้ <span class="code">np.linalg.lstsq</span>
ซึ่งภายในใช้การแยก <b>SVD</b>:</p>
$$ \mathbf{A} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\mathsf{T}
\quad\Longrightarrow\quad
\mathbf{A}^{+} = \mathbf{V}\boldsymbol{\Sigma}^{+}\mathbf{U}^\mathsf{T} $$
<p>เหตุผลเป็นเรื่องเสถียรภาพเชิงตัวเลขล้วนๆ: <b>condition number ของ
\(\mathbf{A}^\mathsf{T}\mathbf{A}\) เท่ากับกำลังสองของ \(\mathbf{A}\)</b></p>
$$ \kappa(\mathbf{A}^\mathsf{T}\mathbf{A}) = \kappa(\mathbf{A})^2 $$
<p>ในการทดสอบที่รันจริงเพื่อสร้างรูปที่ 6 ได้ \(\kappa(\mathbf{A}) \approx 9{,}812\)
ซึ่งแปลว่าถ้าแก้ผ่านสมการปกติจะได้ \(\kappa \approx 9.6\times10^{7}\) — สูญเสียความแม่นยำ
ไปราว 8 หลักทศนิยม จาก 16 หลักที่ double precision มีให้ SVD จึงเป็นทางเลือกที่ถูกต้อง</p>

<figure><img src="figures/06-least-squares.svg" alt="least squares">
<figcaption><b>รูปที่ 6</b> การถดถอยและเศษเหลือ ค่าที่ประมาณได้
\(\hat\beta = 0.43011\), \(\hat\gamma = -1.2815\) เทียบกับค่าจริงที่ใช้สร้างข้อมูล
\(0.429\) และ \(-1.287\) — กู้คืนได้แม่นยำ<br>
<b>หมายเหตุความซื่อสัตย์:</b> ข้อมูลในรูปนี้เป็นข้อมูลจำลอง เพราะไซต์นี้ไม่มีมิเตอร์
จึงไม่มีประวัติ \((I, T, P)\) จริงให้ fit — <b>วิธีการ</b>คือของจริงที่ระบบใช้ แต่<b>ข้อมูล</b>ไม่ใช่</figcaption></figure>

<h3>6.2 ค่าตั้งต้นเชิงฟิสิกส์เมื่อยังไม่มีข้อมูลให้ fit</h3>
<p>เมื่อยังไม่มีประวัติจริง ระบบใช้พารามิเตอร์ที่ derive จากกำลังติดตั้งโดยตรง
โดยบังคับให้ที่สภาวะ STC (1000 W/m², 25°C) กำลังออกเท่ากับกำลังติดตั้งพอดี:</p>
$$ \beta = \frac{P_{\text{rated}}}{1000}, \qquad
\gamma = P_{\text{rated}}\cdot\frac{\tau}{100}, \qquad
c = -\gamma \cdot 25 $$
<p>โดย \(\tau = -0.30\%/°C\) คือสัมประสิทธิ์อุณหภูมิของแผง N-type i-TOPCon
ตรวจสอบได้ว่า \(P(1000, 25) = \beta(1000) + \gamma(25) - 25\gamma = P_{\text{rated}}\) พอดี</p>

<h3>6.3 Ridge regression สำหรับการแก้ไข bias</h3>
<p>ชั้นแก้ไข bias ใช้ <b>Ridge regression</b> (\(\lambda = 1.0\)) แทน OLS
ซึ่งเป็น least squares ที่เติมพจน์ลงโทษขนาดสัมประสิทธิ์:</p>
$$ \hat{\mathbf{w}}_{\text{ridge}} = \arg\min_{\mathbf{w}}
\left\{ \lVert \mathbf{A}\mathbf{w} - \mathbf{b}\rVert_2^2 + \lambda\lVert\mathbf{w}\rVert_2^2 \right\} $$
<p>ซึ่งมีคำตอบรูปปิด:</p>
$$ \hat{\mathbf{w}}_{\text{ridge}} = (\mathbf{A}^\mathsf{T}\mathbf{A} + \lambda\mathbf{I})^{-1}\mathbf{A}^\mathsf{T}\mathbf{b} $$
<p><b>เหตุผลที่จำเป็นที่นี่โดยเฉพาะ:</b> ฟีเจอร์ที่ป้อนเข้าไป — GHI, GHI clear-sky,
cloud index — <b>สัมพันธ์กันสูงมากโดยธรรมชาติ</b> (multicollinearity) ทำให้
\(\mathbf{A}^\mathsf{T}\mathbf{A}\) เกือบ singular และ OLS ให้สัมประสิทธิ์ที่แกว่งรุนแรง
การเติม \(\lambda\mathbf{I}\) ยกค่า eigenvalue ทุกตัวขึ้น \(\lambda\) จึงรับประกันว่าเมทริกซ์
invertible เสมอ:</p>
$$ \operatorname{eig}(\mathbf{A}^\mathsf{T}\mathbf{A} + \lambda\mathbf{I}) = \{\sigma_i^2 + \lambda\} > 0 $$

<figure><img src="figures/07-ridge-path.svg" alt="ridge path">
<figcaption><b>รูปที่ 7</b> Ridge path บนข้อมูลที่ตั้งใจให้ collinear
ที่ \(\lambda\) ต่ำ สัมประสิทธิ์ของสองตัวแปรที่สัมพันธ์กันแยกไปคนละทางอย่างรุนแรง
(+2 และ −1) ทั้งที่ผลรวมของมันเสถียร เมื่อ \(\lambda\) เพิ่ม ทั้งคู่ถูกดึงเข้าหากัน
เส้นประแดงคือ \(\lambda = 1\) ที่ระบบใช้จริง</figcaption></figure>
""")


def render_chapters() -> str:
    out = []
    for c in CH:
        out.append(
            f'<section class="chapter"><h2><span class="chnum">บทที่ {c["num"]}</span>{c["title"]}</h2>{c["body"]}</section>'
        )
    return "\n".join(out)


# ===========================================================================
chapter("7", "การหาค่าเหมาะที่สุด (Optimization)", r"""
<p>ระบบแก้ปัญหา optimization สามแบบที่มีโครงสร้างต่างกันโดยสิ้นเชิง
และเลือกวิธีต่างกันตามโครงสร้างนั้น</p>

<h3>7.1 การหามุมติดตั้งที่ดีที่สุด — ปัญหาสองมิติ ไม่มีอนุพันธ์</h3>
<p>ต้องการหามุมเอียง \(\beta\) และมุมทิศ \(\gamma\) ที่ทำให้พลังงานรายปีสูงสุด:</p>
$$ (\beta^*, \gamma^*) = \arg\max_{\beta \in [0°, 40°],\ \gamma \in [120°, 240°]} \ \sum_{t \in \mathcal{T}} I_{POA}(t; \beta, \gamma)\,\Delta t $$
<p>โดย \(\mathcal{T}\) คือชั่วโมงทั้งหมดใน 12 วันตัวแทน (กลางเดือนของแต่ละเดือน)
ถ่วงน้ำหนักด้วยจำนวนวันจริงของเดือนนั้น</p>

<h4>ทำไมจึงใช้ grid search ไม่ใช่ gradient descent</h4>
<ol>
<li><b>ฟังก์ชันเป้าหมายไม่มีรูปปิด</b> — \(I_{POA}\) ผ่าน Erbs (แบ่งช่วง จึงไม่ต่อเนื่อง
ในอนุพันธ์ที่ \(k_t = 0.22, 0.80\)) และ Hay-Davies แล้วยังมี clipping ที่ \(\cos\theta \le 0\)
อนุพันธ์เชิงวิเคราะห์จึงไม่มี และอนุพันธ์เชิงตัวเลขจะสะดุดที่รอยต่อเหล่านี้</li>
<li><b>พื้นที่ค้นหาเล็กมาก</b> — 21 × 25 = 525 จุด แต่ละจุดใช้เวลาไม่ถึงวินาที
ต้นทุนรวมต่ำกว่าการเขียน optimizer ที่ทนต่อ non-smoothness</li>
<li><b>ต้องการ<i>พื้นผิว</i>ไม่ใช่แค่<i>จุด</i></b> — รายงานต้องบอกว่า &ldquo;เอียงผิดไป 5°
เสียพลังงานเท่าไร&rdquo; ซึ่ง gradient descent ไม่ให้ข้อมูลนี้</li>
</ol>

<figure><img src="figures/10-tilt-surface.svg" alt="tilt surface">
<figcaption><b>รูปที่ 8</b> พื้นผิวเป้าหมาย คำนวณจาก Hay-Davies บน 12 วันตัวแทนจริง
จุดที่ดีที่สุดคือ <b>เอียง 14° หันทิศใต้ (180°)</b><br>
สังเกตว่าพื้นผิว<b>แบนมากบริเวณยอด</b> — เส้น contour ขาว 99% ครอบคลุมช่วงกว้าง
แปลว่าความคลาดเคลื่อนของมุมไม่กี่องศาแทบไม่มีผลต่อพลังงาน ซึ่งเป็นข่าวดีสำหรับไซต์ที่
<b>ยังไม่เคยวัดมุมจริง</b></figcaption></figure>

<div class="callout warn">
<p><b>สิ่งที่พูดได้และพูดไม่ได้จากรูปนี้</b> — <span class="code">assets.yaml</span> ระบุ
<span class="code">tilt_deg: null</span> ทุกโซน คือ<b>ไม่เคยวัดมุมจริง</b>
ดังนั้นประโยคที่ว่า &ldquo;อาร์เรย์ที่สร้างไว้เสียพลังงานไป N kWh/ปี&rdquo; <b>พูดไม่ได้</b>
เพราะไม่รู้ว่ามุมปัจจุบันคือเท่าไร<br>
แต่ <b>ตำแหน่งจุดที่ดีที่สุดพูดได้</b> เพราะขึ้นกับเส้นทางดวงอาทิตย์เท่านั้น ไม่ขึ้นกับสิ่งที่สร้างไว้
และเป็นข้อมูลที่ใช้ออกแบบเฟสขยายในอนาคตได้โดยตรง</p>
</div>

<h3>7.2 การหา IRR — ปัญหาหารากมิติเดียว</h3>
<p>Internal Rate of Return คืออัตราคิดลด \(r^*\) ที่ทำให้มูลค่าปัจจุบันสุทธิเป็นศูนย์:</p>
$$ \text{NPV}(r^*) = \sum_{t=0}^{N} \frac{CF_t}{(1+r^*)^t} = 0 $$
<p>สมการนี้เป็นพหุนามดีกรี \(N\) ใน \(1/(1+r)\) จึงอาจมีรากหลายค่าในทางทฤษฎี
แต่สำหรับกระแสเงินสดที่เปลี่ยนเครื่องหมาย<b>ครั้งเดียว</b> (จ่ายก้อนเดียวตอนต้น
แล้วรับตลอดอายุโครงการ) กฎเครื่องหมายของ Descartes รับประกันว่ามี<b>รากบวกไม่เกินหนึ่งราก</b></p>

<h4>วิธี bisection และเหตุผลที่เลือก</h4>
<p>ระบบใช้ bisection บนช่วง \([-0.5,\ 10.0]\) ทำซ้ำสูงสุด 200 รอบ:</p>
$$ r_{k+1} = \frac{a_k + b_k}{2}, \qquad
\begin{cases}
a_{k+1} = r_k & \text{ถ้า } \operatorname{sign}\text{NPV}(r_k) = \operatorname{sign}\text{NPV}(a_k) \\
b_{k+1} = r_k & \text{มิฉะนั้น}
\end{cases}
$$
<p>อัตราการลู่เข้าเป็น<b>เชิงเส้น</b> โดยความกว้างของช่วงลดลงครึ่งหนึ่งทุกรอบ:</p>
$$ |r_k - r^*| \le \frac{b_0 - a_0}{2^k} $$
<p>Newton-Raphson จะลู่เข้าแบบกำลังสอง (เร็วกว่ามาก) แต่<b>ไม่รับประกันว่าจะลู่เข้า</b>
และต้องใช้อนุพันธ์ ขณะที่ bisection รับประกันเสมอเมื่อมีการเปลี่ยนเครื่องหมาย
สำหรับปัญหาที่ต้องแก้เพียงครั้งเดียวต่อการร้องขอหนึ่งครั้ง ความเร็วไม่ใช่ข้อจำกัด
แต่<b>ความน่าเชื่อถือเป็น</b></p>

<h4>รายละเอียดที่มีผลจริง: ทำไมขอบล่างจึงเป็น −50% ไม่ใช่ −100%</h4>
<p>เมื่อ \(r \to -100\%\) ตัวประกอบคิดลด \(1/(1+r)^t \to \infty\) ทำให้ NPV
ตัดศูนย์ที่อัตราไร้สาระอย่าง −95% ได้ ซึ่ง<b>เป็นรากที่ถูกต้องทางคณิตศาสตร์
แต่ไม่ใช่คำตอบที่ dashboard ควรแสดง</b> ขอบล่างที่ −50% ไกลพอจะบอกได้ว่า
&ldquo;ไม่คุ้มอย่างชัดเจน&rdquo; โดยไม่ตกลงไปในภาวะเอกฐานนั้น</p>

<figure><img src="figures/09-irr-bisection.svg" alt="irr">
<figcaption><b>รูปที่ 9</b> ซ้าย: NPV เป็นฟังก์ชันลดทางเดียวของอัตราคิดลด จึงมีรากเดียว
ได้ IRR ≈ 15.82% ขวา: ความคลาดเคลื่อนลดลงครึ่งหนึ่งทุกรอบ (แกน log เป็นเส้นตรง)
ยืนยันการลู่เข้าเชิงเส้น</figcaption></figure>

<h3>7.3 Pinball loss — optimization ที่ให้ quantile</h3>
<p>โมเดล Sum-k LSTM ให้ช่วงความเชื่อมั่น ไม่ใช่แค่ค่ากลาง ทำได้โดยฝึกด้วย
<b>pinball loss</b> (quantile loss) ซึ่งเป็นฟังก์ชันสูญเสีย<b>ไม่สมมาตร</b>:</p>
$$ \rho_\tau(u) = \begin{cases} \tau u & u \ge 0 \\ (\tau - 1)u & u < 0 \end{cases}
\qquad u = y - \hat{q}_\tau $$
<p><b>ทำไมการทำให้ค่านี้น้อยที่สุดจึงได้ quantile ที่ \(\tau\) พอดี</b>
พิจารณาค่าคาดหมายและหาอนุพันธ์เทียบ \(\hat q\):</p>
$$ \frac{\partial}{\partial \hat q}\,\mathbb{E}[\rho_\tau(Y - \hat q)]
= -\tau\,P(Y > \hat q) + (1-\tau)\,P(Y \le \hat q) = 0 $$
$$ \Longrightarrow\quad P(Y \le \hat q) = \tau $$
<p>ซึ่งคือนิยามของ quantile ที่ \(\tau\) พอดี ระบบใช้ \(\tau = 0.05\) และ \(0.95\)
จึงได้ช่วงความเชื่อมั่น 90%</p>

<figure><img src="figures/08-pinball.svg" alt="pinball">
<figcaption><b>รูปที่ 10</b> Pinball loss ที่สาม \(\tau\) ที่ \(\tau = 0.05\)
การทำนายสูงเกิน (\(u &lt; 0\)) ถูกลงโทษหนักกว่าการทำนายต่ำเกินถึง 19 เท่า
โมเดลจึงถูกผลักให้ทำนายต่ำ ซึ่งเป็นสิ่งที่ต้องการสำหรับขอบล่างของช่วง
ที่ \(\tau = 0.5\) เส้นสมมาตร และได้ค่ามัธยฐาน (คือ MAE หารสอง)</figcaption></figure>
""")

# ===========================================================================
chapter("8", "แบบจำลองพยากรณ์และการแข่งขันระหว่างโมเดล", r"""
<p>ระบบไม่ได้ใช้โมเดลเดียว แต่ใช้สี่ตระกูลตามช่วงเวลาพยากรณ์
เพราะฟิสิกส์ที่ครอบงำแต่ละช่วงต่างกัน</p>

<table class="spec">
<tr><th>ช่วงเวลา</th><th>โมเดล</th><th>ตัวขับหลัก</th></tr>
<tr><td>นาทีข้างหน้า (0–60 นาที)</td><td>CNN-LSTM</td><td>การเคลื่อนที่ของเมฆจากภาพดาวเทียม</td></tr>
<tr><td>ชั่วโมงข้างหน้า (1–6 ชม.)</td><td>LightGBM / Random Forest / Sum-k LSTM</td><td>ความต่อเนื่อง + NWP</td></tr>
<tr><td>วันข้างหน้า (ถึง 72 ชม.)</td><td>NeuralProphet</td><td>NWP + ฤดูกาล</td></tr>
</table>

<h3>8.1 Gradient boosting (LightGBM) — การประมาณอันดับสอง</h3>
<p>Gradient boosting สร้างแบบจำลองเป็นผลรวมของต้นไม้ โดยเพิ่มทีละต้น:</p>
$$ \hat{y}^{(t)} = \hat{y}^{(t-1)} + \eta\, f_t(\mathbf{x}) $$
<p>ในแต่ละรอบเลือก \(f_t\) ที่ทำให้ objective ต่ำสุด โดยกระจาย Taylor อันดับสอง:</p>
$$ \mathcal{L}^{(t)} \simeq \sum_{i=1}^{m}\left[ g_i f_t(\mathbf{x}_i) + \tfrac{1}{2}h_i f_t^2(\mathbf{x}_i) \right] + \Omega(f_t) $$
<p>โดย \(g_i = \partial_{\hat y}\ell(y_i, \hat y^{(t-1)})\) และ
\(h_i = \partial^2_{\hat y}\ell\) เป็นอนุพันธ์อันดับหนึ่งและสอง
สำหรับ squared error ได้ \(g_i = 2(\hat y_i - y_i)\) และ \(h_i = 2\) พอดี</p>
<p>น้ำหนักที่ดีที่สุดของใบไม้ \(j\) มีรูปปิด — นี่คือจุดที่ optimization เกิดขึ้นจริง:</p>
$$ w_j^* = -\frac{\sum_{i \in I_j} g_i}{\sum_{i \in I_j} h_i + \lambda} $$

<h3>8.2 Random Forest — การลดความแปรปรวน</h3>
<p>Random Forest เฉลี่ยต้นไม้ \(B\) ต้นที่ฝึกบน bootstrap sample ต่างกัน:</p>
$$ \hat{y} = \frac{1}{B}\sum_{b=1}^{B} T_b(\mathbf{x}) $$
<p>ถ้าต้นไม้แต่ละต้นมีความแปรปรวน \(\sigma^2\) และมีสหสัมพันธ์ระหว่างกัน \(\rho\)
ความแปรปรวนของค่าเฉลี่ยคือ:</p>
$$ \operatorname{Var}(\hat{y}) = \rho\sigma^2 + \frac{1-\rho}{B}\sigma^2 $$
<p>สมการนี้อธิบายทั้งจุดแข็งและเพดานของวิธี: เทอมที่สองลดลงตาม \(B\) ได้เรื่อยๆ
แต่เทอมแรก \(\rho\sigma^2\) <b>ไม่ลดเลย</b> การสุ่มฟีเจอร์ในแต่ละ split
จึงมีไว้เพื่อลด \(\rho\) โดยเฉพาะ</p>

<h3>8.3 การแข่งขันระหว่างโมเดล</h3>
<p>สำหรับพยากรณ์รายชั่วโมง ระบบฝึกโมเดล 6 ตัวแยกกัน (หนึ่งตัวต่อหนึ่ง lead time
\(k = 1,\dots,6\)) และในแต่ละ lead มีผู้เข้าแข่ง 3 ราย ระบบเลือกผู้ชนะด้วย RMSE
บนชุด hold-out:</p>
$$ \text{model}^*(k) = \arg\min_{M \in \{\text{LGBM},\,\text{RF},\,\text{LSTM}\}} \text{RMSE}_{\text{holdout}}(M, k) $$
<p>ผลคือโมเดลที่ชนะอาจ<b>ต่างกันในแต่ละ lead time</b> ซึ่งเป็นสิ่งที่คาดหวังได้:
ที่ \(k=1\) ความต่อเนื่องยังมีข้อมูลมาก ที่ \(k=6\) NWP มีน้ำหนักกว่า</p>
<p>เว็บแสดง RMSE ของผู้เข้าแข่งทั้งสามพร้อมกัน จึงตรวจสอบการเลือกได้</p>

<h3>8.4 ฟีเจอร์ที่ป้อนเข้าโมเดล</h3>
<p>นอกจาก NWP ดิบ ระบบสร้างฟีเจอร์เชิงฟิสิกส์เพิ่ม ที่สำคัญคือ <b>cloud index</b>:</p>
$$ CI = 1 - \frac{GHI_{\text{observed}}}{GHI_{\text{clear-sky}}} $$
<p>ฟีเจอร์นี้มีคุณสมบัติที่ดีคือ<b>ไม่ขึ้นกับฤดูกาลและเวลาของวัน</b> — ตัวส่วนดูดซับ
วัฏจักรทางดาราศาสตร์ไปหมด เหลือเฉพาะผลของเมฆ ซึ่งเป็นสิ่งที่โมเดลต้องเรียนรู้จริงๆ</p>
""")

# ===========================================================================
chapter("9", "การตรวจสอบความแม่นยำ (Verification)", r"""
<p>บทนี้ตอบคำถามที่ต่างจาก &ldquo;โมเดล fit ข้อมูลฝึกได้ดีแค่ไหน&rdquo; โดยสิ้นเชิง
ระบบเก็บ<b>คำพยากรณ์ที่เผยแพร่ออกไปจริง</b> ไว้ใน <span class="code">forecast_history</span>
แล้วนำมาเทียบเมื่อเวลานั้นมาถึง</p>

<h3>9.1 มาตรวัดจุดเดียว</h3>
$$ \text{MAE} = \frac{1}{n}\sum_{i=1}^{n}\left|\hat{y}_i - y_i\right| $$
$$ \text{RMSE} = \sqrt{\frac{1}{n}\sum_{i=1}^{n}(\hat{y}_i - y_i)^2} $$
$$ \text{MBE} = \frac{1}{n}\sum_{i=1}^{n}(\hat{y}_i - y_i) $$
$$ \text{nRMSE} = \frac{\text{RMSE}}{P_{\text{capacity}}}\times 100\% $$
<p>ทั้งสามตัวแรกมีบทบาทต่างกัน: MAE ทนต่อค่าผิดปกติ RMSE ลงโทษความคลาดเคลื่อนใหญ่หนักกว่า
(จึงไวต่อการพลาดช่วงเมฆผ่าน) ส่วน <b>MBE บอกทิศทาง</b> — ถ้าเป็นบวกอย่างเป็นระบบ
แปลว่าโมเดลมองโลกในแง่ดีเกินไป ซึ่งเป็นข้อมูลคนละอย่างกับขนาดความคลาดเคลื่อน</p>

<h3>9.2 Skill score — เทียบกับ baseline ที่ต้องเอาชนะ</h3>
<p>RMSE เพียงลำพังไม่มีความหมาย ต้องเทียบกับบางอย่าง มาตรฐานในงานพยากรณ์แสงอาทิตย์คือ
<b>persistence</b> (สมมติว่าอีก \(k\) ชั่วโมงข้างหน้าจะผลิตเท่ากับตอนนี้):</p>
$$ \text{SS} = 1 - \frac{\text{RMSE}_{\text{model}}}{\text{RMSE}_{\text{persistence}}} $$
<table class="spec">
<tr><th>SS &gt; 0</th><td>โมเดลเพิ่มข้อมูลจริง</td></tr>
<tr><th>SS = 0</th><td>ไม่ต่างจากการเดาว่าค่าจะคงเดิม</td></tr>
<tr><th>SS &lt; 0</th><td>แย่กว่าการไม่ทำอะไรเลย — ต้องแก้โมเดล</td></tr>
</table>

<div class="callout">
<p><b>รายละเอียดที่มีผลต่อความถูกต้องของตัวเลข</b> — ระบบคำนวณตัวเลขหลัก
<b>เฉพาะชั่วโมงที่มีแดด</b> เท่านั้น เหตุผล: ชั่วโมงกลางคืนทายถูกง่ายมาก
(ค่าจริงเป็น 0 และโมเดลก็ทายใกล้ 0) ถ้ารวมเข้าไปจะทำให้ RMSE ดูดีเกินจริง
และ skill score เฟ้อ เพราะ persistence ก็ทายกลางคืนถูกเหมือนกัน</p>
</div>

<h3>9.3 การประเมินช่วงความเชื่อมั่น</h3>
<p>การรายงานเฉพาะเส้นกลางแล้วอ้างว่ามีช่วงความเชื่อมั่นโดยไม่ตรวจสอบ เป็นการอ้างที่ตรวจไม่ได้
ระบบจึงวัดสามอย่าง</p>

<h4>PICP — ความครอบคลุมจริง</h4>
$$ \text{PICP} = \frac{1}{n}\sum_{i=1}^{n} \mathbb{1}\{L_i \le y_i \le U_i\} \times 100\% $$
<p>ค่านี้ควรเข้าใกล้ <b>90%</b> ตามที่ประกาศไว้ (\(\tau = 0.05\) ถึง \(0.95\))
ถ้าได้ 70% แปลว่าช่วงแคบเกินจริงและระบบกำลังโฆษณาความมั่นใจเกินตัว</p>

<h4>PINAW — ความคมของช่วง</h4>
$$ \text{PINAW} = \frac{1}{n\,(y_{\max} - y_{\min})}\sum_{i=1}^{n}(U_i - L_i)\times100\% $$
<p>จำเป็นต้องมีคู่กับ PICP เพราะ<b>ช่วงที่กว้างอนันต์จะได้ PICP 100% เสมอ</b>
แต่ไร้ประโยชน์ สองค่านี้ต้องอ่านพร้อมกัน</p>

<h4>Pinball loss — proper scoring rule</h4>
<p>ระบบใช้ pinball loss (บทที่ 7.3) เป็นคะแนนรวม <b>ไม่ใช้ CRPS</b>
ทั้งที่ CRPS เป็นที่นิยมกว่า เหตุผลตรงไปตรงมา: CRPS ต้องการการกระจายตัวเต็มรูป
แต่ระบบเผยแพร่เพียง 2 quantile การคำนวณ CRPS จากสองจุดจะเป็น<b>การประมาณที่แต่งตัวเป็นคะแนนจริง</b>
ขณะที่ pinball เป็น proper scoring rule ณ quantile ที่เผยแพร่จริงพอดี</p>

<figure><img src="figures/11-interval-calibration.svg" alt="picp">
<figcaption><b>รูปที่ 11</b> การสอบเทียบช่วง จุดกากบาทแดงคือค่าจริงที่หลุดออกนอกช่วง
สำหรับช่วง 90% ควรมีราว 10% ของจุดที่หลุด — มากกว่านั้นคือช่วงแคบเกิน น้อยกว่านั้นคือกว้างเกิน</figcaption></figure>

<h3>9.4 การแยกตามสภาพท้องฟ้า</h3>
<p>ค่า RMSE ค่าเดียวซ่อนข้อมูลสำคัญว่าความคลาดเคลื่อน<b>มาจากไหน</b>
ระบบจึงจำแนกทุกชั่วโมงด้วย clearness index \(k_t\) แล้วรายงานแยก:</p>
$$ \text{sky}(k_t) = \begin{cases}
\text{overcast} & k_t \le 0.35 \\
\text{partly cloudy} & 0.35 < k_t \le 0.65 \\
\text{clear} & k_t > 0.65
\end{cases} $$
<p>รูปแบบที่คาดหมาย: ฟ้าใสพยากรณ์แม่นมาก (ฟิสิกส์ล้วน) ฟ้าปิดแม่นพอควร
(ต่ำสม่ำเสมอ) ส่วน<b>ฟ้าบางส่วนแย่ที่สุด</b> เพราะเมฆผ่านทำให้กำลังผลิตแกว่งเร็ว
การแยกแบบนี้เปลี่ยน &ldquo;โมเดลแม่น 8%&rdquo; ให้เป็นข้อมูลที่ใช้ปรับปรุงได้จริง</p>
""")

# ===========================================================================
chapter("10", "แบบจำลองทางการเงิน", r"""
<p>ส่วนนี้ตอบคำถามที่มีผลต่อการตัดสินใจจริงของไซต์นี้</p>

<h3>10.1 กระแสเงินสดและการเสื่อมของแผง</h3>
<p>พลังงานที่ผลิตได้ลดลงทุกปีตามการเสื่อมของแผง \(d\):</p>
$$ E_t = E_0 (1 - d)^{t} $$
<p>ระบบใช้ \(d = 0.55\%\)/ปี ซึ่งมาจาก<b>ใบรับประกันจริงของแผง Trina ที่ติดตั้ง</b>
(origin = as-built) ไม่ใช่ค่ามาตรฐานทั่วไป</p>
<p>กระแสเงินสดสุทธิปีที่ \(t\) โดยมีอัตราค่าไฟเฟ้อ \(e\) และค่าดำเนินการเฟ้อ \(f\):</p>
$$ CF_t = \underbrace{E_t \cdot p_0(1+e)^t}_{\text{รายรับ}} - \underbrace{O_0(1+f)^t}_{\text{ค่าดำเนินการ}} - \underbrace{\text{Tax}_t}_{\text{ภาษี}} $$

<h3>10.2 สิทธิประโยชน์ BOI</h3>
<p>โครงการได้รับการส่งเสริมจาก BOI ซึ่งยกเว้นภาษีเงินได้นิติบุคคล
<b>8 ปีในพื้นที่ทั่วไป (GIS, ISB) และ 12 ปีสำหรับ Jetty</b> (ผู้ใช้ยืนยันแล้ว, origin = confirmed):</p>
$$ \text{Tax}_t = \begin{cases} 0 & t \le T_{\text{BOI}} \\ \max(0,\ \text{EBT}_t)\cdot\theta & t > T_{\text{BOI}} \end{cases} $$
<p>โดย \(\theta = 20\%\) คืออัตราภาษีนิติบุคคลมาตรฐานของไทย (ค่าจริง ไม่ใช่ค่าประมาณ)</p>

<h3>10.3 NPV, LCOE และระยะคืนทุน</h3>
$$ \text{NPV} = -C_0 + \sum_{t=1}^{N}\frac{CF_t}{(1+r)^t} $$
<p><b>LCOE</b> คือต้นทุนต่อหน่วยพลังงานตลอดอายุโครงการ
โดย<b>ต้องคิดลดทั้งเงินและพลังงาน</b> — จุดที่มักทำผิด เพราะพลังงานที่ผลิตในปีที่ 20
ไม่ได้มีมูลค่าเท่ากับที่ผลิตปีนี้:</p>
$$ \text{LCOE} = \frac{C_0 + \sum_{t=1}^{N} \dfrac{O_t}{(1+r)^t}}{\sum_{t=1}^{N} \dfrac{E_t}{(1+r)^t}} $$
<p><b>ระยะคืนทุน</b> หาโดย interpolation เชิงเส้นภายในปีที่กระแสเงินสดสะสมตัดศูนย์
จึงได้ค่าเป็นเศษปี (เช่น 6.3 ปี) ไม่ใช่ปัดขึ้นเป็นจำนวนเต็ม</p>

<h3>10.4 ความไม่แน่นอน: P50 / P90</h3>
<p>วงการการเงินพลังงานหมุนเวียนไม่ใช้ค่าเดียว แต่ใช้ <b>exceedance level</b>
โดย P90 หมายถึง &ldquo;พลังงานที่มีโอกาส 90% ที่จะผลิตได้<b>เกิน</b>ค่านี้&rdquo;
ซึ่งเป็นค่าที่<b>ต่ำกว่า</b> P50 (ธนาคารใช้ P90 ในการปล่อยกู้)</p>
<p>สมมติพลังงานรายปีแจกแจงปกติรอบค่า P50:</p>
$$ E_p = E_{50} + \Phi^{-1}(1 - p)\,\sigma, \qquad \sigma = E_{50}\cdot \text{CV} $$
<p>โดย \(\Phi^{-1}\) คือ inverse standard normal CDF (quantile function)
ตัวอย่างค่าที่ใช้บ่อย \(\Phi^{-1}(0.10) = -1.2816\)</p>

<figure><img src="figures/13-exceedance.svg" alt="exceedance">
<figcaption><b>รูปที่ 12</b> ระดับ exceedance ที่ CV = 7% พื้นที่แรเงาเขียวคือความน่าจะเป็น
90% ที่พลังงานจริงจะเกินระดับ P90</figcaption></figure>

<h3>10.5 ห่วงโซ่การสูญเสีย</h3>
<p>การสูญเสียแต่ละชนิดคูณกัน ไม่ใช่บวกกัน — จุดที่มักคำนวณผิด:</p>
$$ \eta_{\text{total}} = \prod_{i}(1 - \ell_i) \ \ne\ 1 - \sum_i \ell_i $$
<p>สำหรับการสูญเสียเจ็ดชนิดที่ระบบใช้ ผลคูณให้ <b>87.06%</b> ขณะที่ผลบวกจะให้ 86.3%
ต่างกัน 0.76 จุดเปอร์เซ็นต์ ซึ่งที่ 850 MWh/ปี คิดเป็นราว 6.5 MWh/ปี</p>

<figure><img src="figures/12-loss-waterfall.svg" alt="losses">
<figcaption><b>รูปที่ 13</b> ห่วงโซ่การสูญเสียแบบคูณกัน จาก 100% เหลือ 87.06%</figcaption></figure>

<div class="callout warn">
<p><b>ค่าที่ยังเป็นค่าประมาณและมีผลต่อผลลัพธ์มากที่สุด</b><br>
<b>CAPEX ฿30,000/kWp</b> และ <b>WACC 8%</b> ยังเป็นค่าประมาณ (origin = placeholder)
ทั้งคู่ ผู้ใช้ทราบและเลือกคงไว้หลังพิจารณาราคาตลาดแล้ว<br>
สิ่งที่ต้องระบุ: ราคาตลาด C&amp;I ในไทยปัจจุบันอยู่ราว ฿20,000–25,000/kWp
ดังนั้น <b>ระยะคืนทุนที่ระบบแสดงน่าจะมองในแง่ร้ายเกินจริง</b> ไม่ใช่ดีเกินจริง</p>
</div>
""")

# ===========================================================================
chapter("11", "เว็บไซต์และตัวอย่างการใช้งาน", r"""
<p>ภาพทั้งหมดในบทนี้เป็น<b>ภาพถ่ายจากระบบที่รันจริง</b> ไม่ใช่ภาพจำลอง
เข้าใช้งานด้วยบัญชีระดับ viewer</p>

<div class="callout">
<p><b>หมายเหตุที่จำเป็นต่อการอ่านภาพ — เวลาที่ถ่าย</b></p>
<p>ภาพทั้งหมดถ่ายที่เวลา <b>12:00 น. ตามเวลาไทย ของวันที่ 1 สิงหาคม 2569</b>
ซึ่งเป็นช่วงที่ระบบผลิตไฟเต็มที่ จึงเห็นค่าการผลิตจริงในภาพ</p>
<p><b>วิธีที่ได้มา และเหตุผลที่ต้องระบุ:</b> เครื่องที่ใช้ถ่ายภาพตั้งนาฬิกาไว้ที่เวลาดังกล่าว
เพราะรอบการทำงานจริงตรงกับเวลากลางคืนที่ไซต์ <b>ตัวเลขทุกตัวในภาพยังคงเป็นผลการคำนวณจริง</b> —
ตำแหน่งดวงอาทิตย์คำนวณจากพิกัดจริงของไซต์ ณ เวลานั้น และรังสีมาจากข้อมูล NWP
ของชั่วโมงนั้นจริง สิ่งที่ถูกเลือกคือ &ldquo;จะถ่ายภาพ ณ เวลาใด&rdquo; ไม่ใช่ตัวเลขที่แสดง</p>
<p>ค่าที่ปรากฏในภาพ ณ เวลานั้น: กำลังผลิต <b>180.6 kW</b>
พลังงานสะสมของวัน <b>585.6 kWh</b> และค่า performance ratio ของแต่ละโซนอยู่ที่
0.70&ndash;0.82 ซึ่งเป็นช่วงที่สมเหตุสมผลสำหรับระบบที่ทำงานปกติ</p>
</div>

<h3>11.1 หน้า Forecast</h3>
<figure><img src="screenshots/s2-forecast.png" alt="forecast page">
<figcaption><b>รูปที่ 14</b> หน้าพยากรณ์ บล็อกตัวเลขใหญ่ด้านบนแสดงสี่ค่าที่อ่านได้จากระยะไกล:
พลังงานที่คาดว่าจะผลิตได้ <span class="num">2,859 kWh</span>,
กำลังผลิตสูงสุด <span class="num">394 kW</span> เวลา 12:00 น.,
ความไม่แน่นอน ณ จุดพีค <span class="num">±20%</span> (ช่วง P10–P90 กว้าง ±78.8 kW),
และระยะเวลาที่จะผลิตไฟ <span class="num">11 ชั่วโมง</span><br>
แถวล่างเป็นค่า ณ ขณะนั้นจริง: กำลังผลิต <span class="num">180.6 kW</span>
และพลังงานสะสมของวัน <span class="num">585.6 kWh</span><br>
สังเกตสัญลักษณ์ ⓘ ข้างหัวข้อ &ldquo;พลังงานที่คาดว่าจะผลิตได้&rdquo; — คือ provenance
inspector ที่กล่าวถึงในหัวข้อ 2.1 กดแล้วจะเห็นสายที่มาทั้งเส้นของตัวเลขนั้น</figcaption></figure>

<h3>11.2 หน้าเต็มของ Forecast</h3>
<figure><img src="screenshots/s3-forecast-full.png" alt="forecast full" class="tall">
<figcaption><b>รูปที่ 15</b> หน้าพยากรณ์แบบเต็ม แสดงกราฟหลักพร้อมแถบช่วงความเชื่อมั่น
กราฟการแข่งขันระหว่างโมเดล ตารางตัวแปรสภาพอากาศ 9 ตัว และแผงตรวจสอบความแม่นยำ</figcaption></figure>

<h3>11.3 หน้า Energy Report</h3>
<figure><img src="screenshots/s4-energy-report.png" alt="energy report" class="tall">
<figcaption><b>รูปที่ 16</b> รายงานพลังงาน แสดงพลังงานรายเดือน การแยกการสูญเสียตามชนิด
(ตรงกับรูปที่ 13) ผลประหยัดค่าไฟ และคาร์บอนที่หลีกเลี่ยงได้</figcaption></figure>

<h3>11.4 หน้า 3D View</h3>
<figure><img src="screenshots/s5-3d-view.png" alt="3d view">
<figcaption><b>รูปที่ 17</b> มุมมองสามมิติ สร้างจากพิกัดมุมแผงที่รังวัดจริง
แสดงตำแหน่งดวงอาทิตย์จริงตามเวลา พร้อมเงาที่คำนวณด้วย shadow mapping
สีของแผงแต่ละแผงสะท้อน solar access ที่คำนวณจากบทที่ 5</figcaption></figure>

<h3>11.5 หน้า Settings</h3>
<figure><img src="screenshots/s6-settings.png" alt="settings" class="tall">
<figcaption><b>รูปที่ 18</b> หน้าตั้งค่า แสดงค่าที่ปรับได้ทั้ง 92 ค่า
แต่ละค่ามีป้าย <span class="code">origin</span> กำกับ (ยืนยันแล้ว / as-built /
งานวิจัย / ค่าประมาณ) ตามที่อธิบายในหัวข้อ 2.1 ป้ายนี้คือกลไกที่ทำให้
ผู้อ่านแยกออกว่าตัวเลขใดเป็นค่าจริงของไซต์และตัวเลขใดเป็นค่าอ้างอิง</figcaption></figure>

<h3>11.6 การใช้งานบนโทรศัพท์</h3>
<figure><img src="screenshots/s7-mobile-forecast.png" alt="mobile" class="phone">
<figcaption><b>รูปที่ 19</b> หน้าเดียวกันบนโทรศัพท์ (390×844 px)
เลย์เอาต์เรียงเป็นคอลัมน์เดียวและกราฟเลื่อนแนวนอนได้</figcaption></figure>
""")

# ===========================================================================
chapter("12", "ข้อจำกัด สรุป และงานต่อไป", r"""
<h3>12.1 ข้อจำกัดที่ต้องระบุ</h3>
<table class="spec">
<tr><th>ข้อจำกัด</th><th>ผลกระทบ</th></tr>
<tr><td><b>ไม่มีมิเตอร์วัดกำลังผลิต</b></td>
<td>ค่า error ทั้งหมดวัดความคลาดเคลื่อนของพยากรณ์อากาศผ่านแบบจำลองฟิสิกส์
ไม่ใช่เทียบการผลิตจริง</td></tr>
<tr><td><b>ไม่เคยวัดมุมเอียงและมุมทิศจริง</b></td>
<td><span class="code">assets.yaml</span> เป็น null ทุกโซน จึงพูดไม่ได้ว่าอาร์เรย์ปัจจุบัน
เสียพลังงานไปเท่าไร (ดูหัวข้อ 7.1)</td></tr>
<tr><td><b>CAPEX และ WACC เป็นค่าประมาณ</b></td>
<td>ระยะคืนทุนน่าจะมองแง่ร้ายเกินจริง เพราะ CAPEX ที่ใช้สูงกว่าราคาตลาด</td></tr>
<tr><td><b>ข้อมูลเมฆเป็นค่าเดียวทั้งไซต์</b></td>
<td>ไม่สามารถทำเงาเมฆรายแผงได้อย่างซื่อสัตย์ — จึงไม่ทำ</td></tr>
<tr><td><b>แบบจำลองเงาพิจารณาแถวติดกันเท่านั้น</b></td>
<td>ประเมินเงาต่ำเกินจริงเมื่อดวงอาทิตย์ต่ำมาก</td></tr>
<tr><td><b>ไม่มีข้อมูลสิ่งกีดขวางภายนอก</b></td>
<td>เงาจากอาคาร/ต้นไม้ไม่ถูกคำนวณ</td></tr>
</table>

<h3>12.2 หลักการที่ควบคุมงานทั้งหมด</h3>
<p>มีกฎเดียวที่ใช้กับทุกส่วนของระบบและของรายงานนี้:
<b>ห้ามแสดงค่าประมาณเป็นค่าที่วัดได้</b> กลไกที่บังคับใช้กฎนี้คือ</p>
<ol>
<li>ทุกค่าที่ปรับได้มี <span class="code">origin</span> กำกับ และแสดงบนหน้าจอ</li>
<li><span class="code">/provenance</span> ตามรอยตัวเลขกลับไปยังต้นทางได้</li>
<li>รายงานความน่าเชื่อถือด้วย<b>ขั้นที่อ่อนที่สุด</b> ไม่ใช่ค่าเฉลี่ย</li>
<li>เมื่อไม่มีข้อมูล แสดง &ldquo;—&rdquo; ไม่ใช่ 0 เพราะสองอย่างนี้ความหมายต่างกัน</li>
<li><b>กฎนี้ใช้กับกราฟิกด้วย</b> — ภาพที่วาดรายละเอียดเกินกว่าข้อมูลรองรับ
ก็คือการปั้นข้อมูล เพียงแต่ปั้นด้วยพิกเซล</li>
</ol>

<h3>12.3 งานต่อไปที่ให้ผลมากที่สุด</h3>
<ol>
<li><b>ติดตั้งมิเตอร์วัดกำลังผลิต</b> — ปลดล็อกการ verify ที่แท้จริง และเปิดทาง
ให้ fit แบบจำลอง PV conversion ด้วยข้อมูลจริง (หัวข้อ 6.1) แทนค่าตั้งต้นเชิงฟิสิกส์</li>
<li><b>วัดมุมเอียงและมุมทิศจริง</b> — ใช้เข็มทิศและ inclinometer ก็เพียงพอ
ทำให้พูดถึงช่องว่างจากมุมที่ดีที่สุดได้</li>
<li><b>ขอตัวเลข CAPEX และ WACC จริงของโครงการ</b> — ทำให้ผลการเงินเป็นของโครงการนี้จริง</li>
<li><b>บันทึกการล้างแผง</b> — เปลี่ยนค่า soiling จาก literature เป็นค่าที่วัดได้</li>
</ol>

<h3>12.4 สรุป</h3>
<p>งานนี้สร้างระบบที่เชื่อมคณิตศาสตร์สี่สาขาเข้าด้วยกันบนไซต์จริงขนาด 429 kWp:
<b>พีชคณิตเชิงเส้น</b>สำหรับเรขาคณิตและการถดถอย (บทที่ 3, 6),
<b>เรขาคณิตวิเคราะห์และดาราศาสตร์</b>สำหรับรังสีและเงา (บทที่ 4, 5),
<b>การหาค่าเหมาะที่สุด</b>ในสามรูปแบบที่ต่างกัน (บทที่ 7),
และ<b>สถิติกับการเงินเชิงปริมาณ</b>สำหรับการประเมินและการตัดสินใจ (บทที่ 9, 10)</p>
<p>สิ่งที่แยกงานนี้จาก dashboard พลังงานแสงอาทิตย์ทั่วไปไม่ใช่จำนวนโมเดล
แต่คือ<b>การที่ทุกตัวเลขบอกได้ว่าตัวเองมาจากไหนและเชื่อถือได้แค่ไหน</b>
ซึ่งเป็นคุณสมบัติที่ตรวจสอบได้จากตัวระบบเอง ไม่ใช่คำกล่าวอ้างในเอกสาร</p>
""")
