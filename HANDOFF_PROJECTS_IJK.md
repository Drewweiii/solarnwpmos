# งาน I / J / K — Demand Charge, PV Recycling, Power Flow

> เขียน 2026-07-25 โดย **Track 1 (เนื้อหาเชิงวิชาการ)** ตามที่ user สั่งว่า
> *"ทำงานค้าง ส่วนนี้ให้เพื่อนทำนะ แจ้งเพื่อนด้วย"*
>
> สามหัวข้อนี้เกิดจากคำถามของ user เอง (*"เสนอนวัตกรรมใหม่ๆ พวก powerflow ems
> highvolt pv recycle ดูบ้างดีไหม"*) ผมสำรวจ codebase ให้เสร็จแล้วว่าแต่ละอันทำได้แค่ไหน
> **ตัวเลขทุกตัวในไฟล์นี้ผมรันจริง/เปิดไฟล์จริงมาแล้ว ไม่ได้ประมาณ** — เอาไปใช้ต่อได้เลย
> ไม่ต้องสำรวจซ้ำ
>
> repo นี้ **ปิด GitHub Issues** เลยใช้ไฟล์ที่ root แทน (เหมือน `WORK_SPLIT.md`)

---

## ตัวเลขพื้นฐานของไซต์ที่ทั้งสามงานใช้ร่วมกัน

รันจาก `nongfab_common.assets.load_assets()` และ `nongfab_simulation.tou.zone_tou_split()`
เมื่อ 2026-07-25 — ไม่ต้องรันซ้ำ:

| ค่า | ตัวเลข | ที่มา |
|---|---|---|
| กำลังติดตั้ง DC | **429.0 kWp** | GIS 60.06 + ISB 140.14 + Jetty 228.8 |
| กำลังอินเวอร์เตอร์ AC | **400 kW** | GIS 50 + ISB 150 + Jetty 200 |
| จำนวนแผง | **600 แผ่น** | GIS 84 + ISB 196 + Jetty 320 |
| ผลผลิตรายปี | **850,109 kWh** | Peak 575,390 / Off-Peak 274,719 (project G) |
| โหลดของคลัง | **13,500 kW** เฉลี่ย | `assets.yaml` — user ยืนยันเอง 2026-07-23 |
| พลังงานที่คลังใช้/ปี | **118,260,000 kWh** | 13,500 kW × 8,760 h |
| ค่าไฟคลัง/ปี | **~300 ล้านบาท** | `assets.yaml` — user ยืนยันเอง 2026-07-23 |
| **ค่าไฟเฉลี่ยจริงที่คลังจ่าย** | **฿2.5368/kWh** | 300 MTHB ÷ 118.26 GWh |
| **โซลาร์คิดเป็นสัดส่วนของโหลด** | **0.72%** | 850,109 ÷ 118,260,000 |

**สองบรรทัดสุดท้ายคือหัวใจของงาน I** — อ่านต่อ

---

# I. Demand Charge — ค่าความต้องการพลังไฟฟ้า (แนะนำให้ทำก่อน)

## ทำไมถึงเป็นช่องโหว่ที่ใหญ่ที่สุดที่เหลืออยู่

ค่าไฟของผู้ใช้ไฟรายใหญ่ในไทย (PEA/MEA ประเภท 4) มี **สองก้อน** ไม่ใช่ก้อนเดียว:

1. **ค่าพลังงานไฟฟ้า** (฿/kWh) — คิดตามหน่วยที่ใช้
2. **ค่าความต้องการพลังไฟฟ้า / Demand Charge** (฿/kW) — คิดจาก **พีค 15 นาทีที่สูงที่สุดของเดือน**
   ไม่เกี่ยวกับยอดใช้รวมเลย

**ทั้งเว็บนี้โมเดลแค่ก้อนที่ 1** ผม grep คำว่า `demand_charge` / `demand charge` /
`ค่าความต้องการ` / `peak_demand` ทั่ว `api/src`, `financial/src`, `simulation/src`,
`forecast/src`, `libs/`, `config/` แล้ว **เจอที่เดียวในทั้ง repo**:

```
web/src/lib/assistantContent.ts:80
  'โรงงานขนาดใหญ่บางประเภทอาจมีค่าความต้องการพลังไฟฟ้าสูงสุด (Demand Charge)
   เพิ่มอีกด้วย - คิดจาก "พีคการใช้ไฟสูงสุด" ในรอบเดือน ไม่ใช่แค่ยอดใช้รวมครับ'
```

คือ **AI assistant ของเว็บอธิบาย demand charge ให้ผู้ชมฟัง แต่ตัวเว็บเองไม่ได้คำนวณมันเลย**
`green_savings.py` และ `financial/` ตีมูลค่าทุก kWh ที่ผลิตได้ด้วยอัตราค่าพลังงานล้วน

## หลักฐานเชิงตัวเลขที่ชี้ว่ามันสำคัญจริง

ค่าไฟเฉลี่ยที่คลังจ่ายจริงคือ **฿2.5368/kWh** แต่อัตราค่าพลังงาน TOU Peak ที่เว็บใช้อยู่คือ
**฿4.1025/kWh** — ต่ำกว่ากันเยอะมาก แปลว่าโครงสร้างค่าไฟจริงของที่นี่ไม่ใช่แค่ "เอา kWh คูณ 4.1025"
ตัวเลข ฿2.5368 นี้เป็นก้อนรวม (ค่าพลังงาน peak+off-peak + demand charge + Ft + ค่าบริการ)
หารด้วยหน่วยทั้งหมด → **มีข้อมูลพอที่จะย้อนหา sanity check ได้ว่า demand charge กินสัดส่วนเท่าไร**

## สมมติฐานหลักที่ต้องพิสูจน์ (นี่คือของจริงที่ยังไม่มีใครตรวจ)

คลัง LNG เดินเครื่อง **24 ชั่วโมง 7 วัน** โหลดค่อนข้างแบน (13-14 MW ตลอด)
ถ้าโหลดแบนจริง **พีคของเดือนอาจเกิดตอนกลางคืนก็ได้** — และถ้าพีคเกิดตอนกลางคืน
**โซลาร์ลดค่า demand charge ไม่ได้เลยแม้แต่บาทเดียว**

ขนาดของผลกระทบ: 429 kWp เทียบกับโหลด 13,500 kW = **มากที่สุดก็ 3.2% ของพีค**
และนั่นคือกรณีที่พีคเกิดตอนเที่ยงวันฟ้าใสพอดีเท่านั้น

**ถ้าสมมติฐานนี้เป็นจริง มันคือข้อค้นพบด้านการลงทุนที่มีน้ำหนักมาก** และตอนนี้ยังไม่มีใครตรวจ

## ข้อจำกัดที่ต้องพูดตรงๆ บนหน้าจอ

**ไซต์นี้ไม่มี load profile รายชั่วโมง/ราย 15 นาทีของคลัง** มีแค่ค่าเฉลี่ย 13.5 MW
เพราะฉะนั้น **ห้ามสร้างกราฟโหลดปลอมขึ้นมา** (ผิดกฎข้อ 1 ของ repo นี้)

## สิ่งที่ทำได้จริงโดยไม่ต้องมี load profile — และควรทำแบบนี้

อย่าตอบว่า "ประหยัดได้ X บาท" ให้ตอบเป็น **เส้นความไว (sensitivity curve)** แทน:

> "ถ้าพีคของเดือนเกิดเวลา H นาฬิกา โซลาร์จะลดพีคได้ P kW และประหยัด demand charge
>  ได้ D บาท/เดือน" — พล็อต H ตั้งแต่ 00:00 ถึง 23:00

แบบนี้ **ทุกค่าเป็นของจริงหมด** (กำลังผลิตของโซลาร์ ณ ชั่วโมงนั้นมาจากโมเดลเดียวกับที่เว็บเผยแพร่)
สิ่งเดียวที่ไม่รู้คือ H ซึ่งเราไม่เดา แต่แสดงทุกค่าให้ user เลือกอ่านเอง
และพอ user ได้บิลจริงมาวันหนึ่ง ก็แค่ชี้ว่าพีคอยู่ชั่วโมงไหน อ่านคำตอบจากกราฟได้ทันที

ค่าที่ควรคำนวณเพิ่ม (ทั้งหมดเป็นของจริง ไม่ต้องสมมติ):
- **กำลังผลิตต่ำสุดในหน้าต่าง Peak (09:00-22:00)** ของแต่ละเดือน = "firm capacity" ที่พึ่งได้จริง
  → น่าจะเป็น **0 kW** เพราะหน้าต่าง peak ลากถึง 22:00 ซึ่งมืดแล้ว **นี่แหละคือคำตอบ**:
  demand charge คิดจากพีคเดียวของเดือน ถ้ามีสักนาทีเดียวที่โหลดสูงตอนโซลาร์ไม่ผลิต
  การลดพีคเป็นศูนย์
- **P90 ของกำลังผลิตรายชั่วโมง** — ค่าที่ผลิตได้เกินนี้ 90% ของวัน ใช้เป็นขอบเขตบนที่พึ่งได้
- **ส่วนแบ่ง demand charge ในบิลจริง** — ย้อนจาก ฿2.5368/kWh เทียบกับอัตราค่าพลังงานที่ประกาศ

## แนวทางเขียนโค้ด

- โมดูลใหม่: `financial/src/nongfab_financial/demand_charge.py`
  (ห้ามใส่ใน `simulation/` — มันเป็นเรื่องเงิน ไม่ใช่ฟิสิกส์)
- ใช้ `nongfab_simulation.tou._representative_day_irradiance_temp` +
  `simulate_zone_baseline` แบบเดียวกับที่ `tou.py` ใช้ → ตัวเลขจะเทียบกับที่เว็บเผยแพร่ได้ตรงๆ
  (ดู `tou.py` เป็นตัวอย่าง มันสั้นและทำแบบเดียวกันเป๊ะ)
- route ใหม่: `api/src/nongfab_api/routes_demand_charge.py` แล้ว register ใน `main.py`
- panel ใหม่บน `/financial` (ไม่ใช่ `/forecast` — `/forecast` แน่นแล้ว 5 panel ใหม่จากกะที่แล้ว)
- **อัตรา demand charge ต้องเป็น setting ไม่ใช่ค่าคงที่ในโค้ด** เพิ่ม entry ใน
  `settings_registry.py` กลุ่ม `green` เช่น `green.demand_charge_thb_per_kw`
  ตั้ง `origin="placeholder"` ไว้ก่อนจนกว่า user จะยืนยันว่าคลังอยู่อัตราไหน
  (ประเภท 4.1 TOD กับ 4.2 TOU คนละอัตรา และยังแยกตามระดับแรงดันอีก)
- ⚠️ **ต้องถาม user ก่อน** ว่าคลังใช้อัตราประเภทไหนและแรงดันเท่าไร
  อย่าเดาแล้วเผยแพร่ตัวเลขเงิน

---

# J. PV Recycling / End-of-Life — ทำได้เลย ไม่ติดข้อมูล

## ทำไมทำได้

ต่างจาก C (เงา ray-trace) ที่ติดเพราะไม่มีพิกัดถัง LNG — งานนี้ **ข้อมูลครบแล้ว**:

| มีอะไร | ค่า | ที่มา |
|---|---|---|
| จำนวนแผงทั้งหมด | **600 แผ่น** | `assets.yaml` (84 + 196 + 320) |
| รุ่นแผง | **Trina Vertex N TSM-NEG21C.20** | `assets.yaml` `module_detail.name` |
| เทคโนโลยี | N-type i-TOPCon **dual glass** (bifacial) | `module_detail.technology` |
| ขนาดต่อแผ่น | **2384 × 1303 × 33 mm** | `module_detail.dimensions_mm` |
| **พื้นที่กระจกรวม** | **1,863.8 m²** | 2.384 × 1.303 × 600 |
| กำลังต่อแผ่น | 715 W | `module_detail.power_w` |
| อายุที่โมเดลใช้ | ดูอายุโครงการใน `financial/` + `degradation_pct_per_year` = 0.4%/ปี | ผมเพิ่งแก้เป็นค่าตามใบรับประกัน Trina จริง |

ต้องหาเพิ่มแค่สองอย่าง และหาได้จากแหล่งเปิด:
1. **น้ำหนักต่อแผ่น** — จาก datasheet Trina รุ่นนี้โดยตรง (dual glass 2384×1303 หนัก ~38 kg)
   ⚠️ **ต้องเปิด datasheet ยืนยัน** อย่าใส่ตัวเลขจากความจำ
2. **องค์ประกอบวัสดุของแผง c-Si** (กระจก / อะลูมิเนียม / ซิลิคอน / ทองแดง / เงิน / โพลิเมอร์)
   — มีงานวิจัยตีพิมพ์เยอะ (IRENA-IEA PVPS *End-of-Life Management: Solar PV Panels* เป็นตัวอ้างอิงมาตรฐาน)
   สัดส่วนคร่าวๆ ของ c-Si คือกระจก ~70% โดยน้ำหนัก **แต่ต้องอ้างอิงจริง ไม่ใช่จำมา**
   หมายเหตุ: รุ่นนี้เป็น **dual glass ไม่มีเฟรมอะลูมิเนียมแบบเดิม** → สัดส่วนไม่เหมือนโมดูลมาตรฐาน
   **อย่าใช้สัดส่วนของโมดูล framed มาตรง ๆ**

## 🇹🇭 นโยบาย Thailand-first — ข้อนี้สำคัญมากกับงานนี้

`CLAUDE.md` กำหนดว่าข้อมูลที่ผูกกับภูมิภาคต้องใช้ของไทยก่อนเสมอ งานนี้ชนเต็มๆ:
- **กฎหมาย/ระเบียบ**: ต้องดูสถานะร่าง พ.ร.บ. การจัดการซากผลิตภัณฑ์ไฟฟ้าและอิเล็กทรอนิกส์ (WEEE) ของไทย
  และประกาศของกรมควบคุมมลพิษ / กรมโรงงานอุตสาหกรรม **ไม่ใช่ EU WEEE Directive**
- **ผู้รับรีไซเคิล**: ต้องหาว่าในไทยมีโรงรีไซเคิลแผงโซลาร์ที่ได้รับอนุญาตหรือยัง (สถานะจริงเป็นอย่างไรก็รายงานอย่างนั้น
  ถ้ายังไม่มีก็บอกว่ายังไม่มี — นั่นคือข้อค้นพบ ไม่ใช่ความล้มเหลว)
- **ถ้าจำเป็นต้องใช้ข้อมูล/ราคา/ระเบียบของประเทศอื่น ต้องหยุดถาม user ก่อน** ห้ามใส่เงียบๆ

## ผลลัพธ์ที่ควรได้

1. **ตารางวัสดุที่กู้คืนได้** — กี่ตันของกระจก / อะลูมิเนียม / ทองแดง / เงิน / ซิลิคอน
2. **มูลค่าซาก (terminal value) ในกระแสเงินสด** — ตอนนี้ `financial/model.py` น่าจะจบที่ปีสุดท้าย
   โดยไม่มีมูลค่าซาก ซึ่ง**อาจติดลบก็ได้** (ค่ากำจัดแพงกว่ามูลค่าวัสดุ) ทั้งสองทางล้วนเป็นคำตอบที่มีประโยชน์
   → ต่อเข้า NPV/IRR ที่มีอยู่แล้ว
3. **panel ESG** บนเว็บ — กี่ตันไม่ต้องไปฝังกลบ เทียบกับคาร์บอนที่ประหยัดได้ตลอดอายุ
4. **carbon payback / EROI** ถ้ามีแรงเหลือ — คาร์บอนที่ใช้ผลิตแผง เทียบกับคาร์บอนที่มันช่วยลด
   (ต่อยอดจาก `/grid/carbon` ที่มี EF ของกริดไทยจริงแล้ว: กกพ. 0.4758 kgCO₂/kWh)

## แนวทางเขียนโค้ด

- โมดูลใหม่: `financial/src/nongfab_financial/end_of_life.py`
- ค่าองค์ประกอบวัสดุใส่เป็น `settings_registry.py` กลุ่มใหม่ `recycling`
  ตั้ง `origin="literature"` ทุกตัว พร้อมอ้างอิงในคอมเมนต์
- ราคาวัสดุรีไซเคิล (ถ้าจะใส่) ต้อง `origin="placeholder"` จนกว่าจะมีใบเสนอราคาจริงของไทย

---

# K. Power Flow / High Voltage — **ติดข้อมูลบางส่วน** แต่มีชิ้นที่ทำได้เลย

## ส่วนที่ติด (อย่าเพิ่งเริ่ม)

Power flow เต็มรูปแบบ (load flow, voltage rise, short-circuit) **ทำไม่ได้** เพราะ
`config/assets.yaml` **ไม่มี topology ไฟฟ้าเลย** ผมเปิดดูทั้งไฟล์แล้ว มีแค่:

```yaml
sld_available: true       # GIS
sld_available: true       # ISB
sld_available: "phase1"   # Jetty
```

คือ**บอกแค่ว่า "มี SLD อยู่นะ" ไม่ได้เก็บเนื้อหาของ SLD ไว้** ไม่มีหม้อแปลง ไม่มีฟีดเดอร์
ไม่มีบัสบาร์ ไม่มีระดับแรงดัน ไม่มีค่าอิมพีแดนซ์ ไม่มีจุดเชื่อมกับกริด PEA

และต่อให้มี ผลก็อาจจืด: 429 kWp เทียบโหลด 13,500 kW → **ไม่มีการจ่ายไฟย้อนเข้ากริดเลย**
แรงดันแทบไม่ขยับ ซึ่งเป็นคำตอบที่ถูกต้องแต่ไม่น่าตื่นเต้น

**ต้องขอจาก user ถ้าจะทำเต็มรูปแบบ**: ระดับแรงดันของบัสที่โซลาร์เชื่อมเข้า, พิกัด+impedance
ของหม้อแปลง, ขนาด/ความยาว/ชนิดสายของแต่ละเส้น, จุดเชื่อมต่อกับ PEA
(SLD มีอยู่จริงในมือ user แค่ยังไม่ได้ถอดลง yaml)

## ✅ ส่วนที่ทำได้เลย — และมันเป็นบั๊กจริง ไม่ใช่ของเล่น

**ค่าสูญเสียในสายที่ไซต์นี้ใช้อยู่ตอนนี้ไม่ได้มาจากความยาวสายจริงเลย**

`simulation/src/nongfab_simulation/loss_model.py`:
```python
DEFAULT_DC_WIRING_PCT = 2.0     # ค่า generic ของ PVWatts
DEFAULT_CONNECTIONS_PCT = 0.5
```
— ใช้ค่าเดียวกันหมดทั้ง 3 โซน และ **ไม่มีเทอมค่าสูญเสียในสาย AC เลยแม้แต่ตัวเดียว**

แต่ `config/assets.yaml` เก็บความยาวจริงไว้แล้ว:
```yaml
interconnection_points:
  - id: EL3,  distance_from_isb_m: 1130   # Phase 1 เชื่อมตรงนี้ -> JB-016A -> ISB
  - id: EL5,  distance_from_isb_m: 1680   # Phase 1.5
  - id: EL9,  distance_from_isb_m: 2816
design_constraints:
  dc_voltage_drop_max_pct_of_vmp: 3
  ac_voltage_drop_max_pct: 5
```

**1,130 เมตร** คือระยะสายจาก Jetty กลับมา ISB ซึ่งยาวมาก และ design constraint เองอนุญาตให้
แรงดันตก AC ได้ถึง **5%** — มากกว่าค่าสูญเสียในสายทั้งหมดที่โมเดลคิดอยู่ (2%) ถึง 2.5 เท่า
แปลว่า **ผลผลิตของ Jetty ที่เว็บเผยแพร่อยู่ตอนนี้อาจสูงเกินจริง**

### สิ่งที่ทำ
- คำนวณค่าสูญเสีย I²R จริงจากความยาวสาย + กระแสรายชั่วโมง (ไม่ใช่ค่าคงที่)
  → ค่าสูญเสียในสายแปรผันตามกำลังยกกำลังสอง ตอนเที่ยงเสียเยอะกว่าตอนเช้ามาก
  ซึ่งค่าคงที่ 2% มองไม่เห็น
- ทำให้ `dc_wiring_pct` เป็น **per-zone** แทนค่าเดียวทั้งไซต์
- Jetty ควรมีค่าสูญเสียสูงกว่า GIS/ISB อย่างมีนัยสำคัญ ถ้าผลออกมาไม่ต่างแปลว่าคำนวณผิด
- ⚠️ **ต้องขอ user**: ขนาดสาย (mm²) และชนิดตัวนำของเส้น 1,130 m
  ถ้าไม่มี ให้ทำแบบ **reverse-engineer**: "ถ้าออกแบบให้แรงดันตกพอดี 3%/5% ตาม design constraint
  ค่าสูญเสียจะเป็นเท่านี้" — ใช้ข้อจำกัดที่ user ให้มาเองเป็นสมมติฐาน **ตรงไปตรงมากว่าการเดาขนาดสาย**

---

## ลำดับที่แนะนำ

1. **I (Demand Charge)** — ผลกระทบสูงสุด ข้อมูลครบ แตะแค่ `financial/` + route ใหม่
2. **J (PV Recycling)** — ข้อมูลครบ ทำคู่ขนานได้ ไม่ชนกับใคร
3. **K ส่วนสายไฟ** — เป็นบั๊กจริง แต่แตะ `loss_model.py` ซึ่งกระทบตัวเลขทั้งเว็บ **ถาม user ก่อนเสมอ**
4. **K ส่วน power flow เต็ม** — อย่าเพิ่งเริ่ม รอ SLD

---

## ⛔ ห้ามแตะ (Track 1 ถืออยู่ / เพิ่งแก้ไป)

ไฟล์ที่กะ 2026-07-25 เพิ่งสร้างหรือแก้ ยังไม่ได้ verify บนเบราว์เซอร์จริง:

```
features/src/nongfab_features/poa.py            (ใหม่)
features/src/nongfab_features/bifacial.py       (ใหม่)
simulation/src/nongfab_simulation/tilt_optimizer.py   (ใหม่)
simulation/src/nongfab_simulation/dc_ac_ratio.py      (ใหม่)
simulation/src/nongfab_simulation/tou.py              (ใหม่)
simulation/src/nongfab_simulation/pipeline.py         (แก้: ใส่ POA transposition)
api/src/nongfab_api/grid_carbon.py + routes_grid_carbon.py
api/src/nongfab_api/routes_orientation.py / routes_dc_ac.py / routes_bifacial.py / routes_tou.py
web/src/components/{GridCarbon,Orientation,DcAc,Bifacial,Tou}Panel.tsx
```

`financial/model.py` **แตะได้** สำหรับงาน I และ J (เพิ่ม demand charge + terminal value)
แต่ระวังงาน **B (P50/P90 Monte Carlo)** ที่ยังค้างอยู่ในไฟล์เดียวกัน — ถ้าจะทำ B ด้วย ทำให้จบทีเดียว

---

## กติกาที่พลาดแล้วเจ็บ (จดจากที่พลาดมาแล้วจริงๆ)

- **ด่านฝั่ง web มี 3 ตัว ไม่มีตัวไหนครอบตัวอื่น** ต้องรันครบก่อน push:
  1. `npm run lint` (oxlint) — จับ syntax ที่อีก 2 ตัวปล่อยผ่าน
  2. `npx vitest run`
  3. `npm run build` ← ต้องตัวนี้ (`tsc -b && vite build`) **ไม่ใช่** `npx tsc --noEmit`
     เพราะ `tsc -b` ตรวจไฟล์เทสด้วย — CI #172/#173 แดงเพราะ fixture ในเทสไม่มีฟิลด์ใหม่
  → **ฟิลด์ใหม่ใน response type ให้ใส่ `?` เสมอ** ไม่งั้น fixture เก่าทุกตัวพัง
- **ห้ามใส่ type ให้พารามิเตอร์ของ recharts formatter** — recharts widen เป็น
  `ValueType | undefined` ให้ narrow ด้วย `typeof` ในบอดี้แทน (พลาดมาแล้ว 3 ครั้ง)
- ต้องรัน `ruff check` เองก่อน push (sandbox ไม่รันให้)
- รัน pytest **ทีละ package** (basename ซ้ำข้าม package ทำให้ collect error)
- **มี `lru_cache` 4 ตัวที่ `settings_service.apply_effective_settings()` ต้อง `cache_clear()`**
  ถ้าเพิ่มตัวที่ 5 ต้องไปเพิ่มในนั้นด้วย ไม่งั้นแก้ setting แล้วตัวเลขไม่ขยับ:
  `annual_shading_loss_pct` · `_profile_for_day` · `optimise_zone` · `analyse_zone`
- ค่าคงที่ใหม่ควรใส่ `settings_registry.py` — เพิ่ม 1 entry ได้ช่องกรอกในหน้า Settings อัตโนมัติ
  (ตอนนี้ **88 ค่า / 10 กลุ่ม**)

## กฎเหล็กเรื่องความซื่อสัตย์ของข้อมูล

ห้ามนำเสนอค่าที่แต่งขึ้นว่าเป็นของจริง ทุกค่าต้องมี `origin`
(`confirmed` / `as-built` / `literature` / `placeholder` / `tuning` / `derived`)
ถ้าไม่มีข้อมูลให้แสดง "ยังไม่มีข้อมูล" ตรงๆ ห้ามเติมเลขให้กราฟดูสวย
และ **ไซต์นี้ไม่มีมิเตอร์วัดกำลังผลิตจริง** — เอาผลไปเทียบกับการผลิตจริงไม่ได้ ต้องเขียนข้อจำกัดนี้บนหน้าจอ

**ถามก่อนตัดสินใจเสมอ** โดยเฉพาะถ้าผลจะทำให้ payback/NPV ที่เผยแพร่อยู่ขยับ
และถ้าประเมินว่างานยาวจนเครดิตอาจหมดกลางทาง ให้เตือน user **ก่อน** เริ่ม

จบงานแล้วเขียน Handoff Report ภาษาไทย 3 หัวข้อ + แท็ก Track แล้ว **append** ลง `HANDOFF.md`
