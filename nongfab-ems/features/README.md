# Module 3 — Feature Store

Not started. Will hold: pvlib clear-sky irradiance (Ineichen) + solar zenith
angle, auto-lag features, quality control (curtailment/degradation
detection), daytime filtering, and multi-step training frame assembly. Reads
`config/assets.yaml` as the source of truth for plant geometry, same as
`ingestion/`.
