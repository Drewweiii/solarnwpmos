"""Feature store & preprocessing (Module 3) for Nong Fab EMS.

Pure, DB-independent feature-engineering functions operating on pandas
DataFrames - so they're unit-testable in isolation and reusable once
Module 1 (cloud obs) and Module 2 (NWP) have accumulated enough history to
feed them for real.
"""

__version__ = "0.1.0"
