# Module 4 — Forecast Engine

Not started. Will hold: minute-ahead CNN-LSTM (neuralforecast), hour-ahead
LightGBM (Optuna-tuned), day-ahead NeuralProphet, the PV conversion model,
and the MLflow registry/retraining pipeline.
