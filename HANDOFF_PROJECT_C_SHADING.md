# โปรเจกต์ใหญ่ส่งต่ออีกบัญชี — เงาจริงแบบ ray-trace จากเรขาคณิตของไซต์

> เขียนไว้ 2026-07-25 · เป็นสเปกฉบับเต็มของงาน **C** ใน `WORK_SPLIT.md`
> เขียนแบบให้เริ่มได้จากศูนย์ ไม่ต้องไล่อ่านประวัติทั้งหมดก่อน
>
> (อยากเปิดเป็น GitHub Issue แต่ repo นี้ปิด Issues ไว้ เลยเป็นไฟล์ที่ root)

## ทำไมงานนี้ถึงเป็นงานใหญ่ที่คุ้มที่สุดที่เหลืออยู่

ตอนนี้ค่าการสูญเสียจากเงาของทั้งระบบมาจาก `annual_shading_loss_pct` ซึ่งเป็น
**สูตรเชิงวิเคราะห์ที่รู้จักแค่ "แถวแผงบังแถวแผงด้วยกันเอง"** (row-to-row shading)

ปัญหาคือไซต์นี้ไม่ใช่ทุ่งโล่ง — มันคือ **คลัง LNG** ที่มีถังเก็บขนาดมหึมา อาคาร
กระบวนการ และท่าเทียบเรืออยู่รอบๆ แผง **สูตรปัจจุบันมองไม่เห็นสิ่งเหล่านี้เลยแม้แต่นิดเดียว**
แปลว่าค่า loss ที่ใช้อยู่อาจต่ำกว่าความจริงมากในบางโซน และเราไม่มีทางรู้ว่าโซนไหน

ผลกระทบเป็นลูกโซ่: เงา → พลังงานรายปี → **payback / NPV / LCOE** ที่หน้า Financial
เผยแพร่ และ → แผนขยายกำลังผลิตว่าควรวางแผงตรงไหน

และของดีคือ **เรขาคณิตที่ต้องใช้มีอยู่ในระบบแล้ว** (`config/assets.yaml` + ฉาก 3D)
งานนี้จึงไม่ต้องหาข้อมูลใหม่จากไหนเลย ต่างจากงานอื่นๆ ที่ติดเรื่องข้อมูล

## ขอบเขตงาน

### 1. สร้างโมดูลคำนวณเงาแบบเรขาคณิต (แกนหลัก)

เสนอให้เป็น pure package ไม่มี state — แนะนำ `simulation/src/nongfab_simulation/shading_raytrace.py`
(อยู่ใน `simulation/` เพราะเป็นฟิสิกส์ ไม่ใช่ชั้น API)

อินพุต:
- เรขาคณิตสิ่งกีดขวางจาก `config/assets.yaml` (ถัง LNG, อาคาร, ท่าเทียบเรือ)
  — **ไปดูก่อนว่า yaml มีความสูง/ตำแหน่งครบไหม** ถ้าขาดต้องเติม และถ้าต้องเดา
  ให้ทำเป็นค่าที่กรอกเองได้ + ติดป้าย origin ตามแบบแผนของ repo นี้
- ตำแหน่งดวงอาทิตย์รายชั่วโมงทั้งปี — **ใช้ของที่มีอยู่แล้ว**
  `nongfab_features.clearsky.compute_clearsky_and_position` ที่พิกัดจริงของหนองแฟบ
  (อย่าเขียน solar position ขึ้นมาใหม่ ของเดิมใช้ pvlib และถูกต้องแล้ว)

เอาต์พุตที่ควรได้:
- shading fraction ต่อโซน ต่อชั่วโมง (8760 ค่า)
- ค่า loss รายปีต่อโซน เอาไปแทน/เสริม `annual_shading_loss_pct`
- ถ้าไหว: heatmap รายแผง/ราย sub-array เพื่อเอาไปวาดใน 3D

### 2. เอาไปต่อกับของเดิม

- `simulation/loss_model.py` — `shading_pct` ต้องรับค่าจากตัวใหม่ได้
- ⚠️ **gotcha ที่จะทำให้เสียเวลาถ้าไม่รู้**: `annual_shading_loss_pct` เป็น
  `lru_cache` ต้องเรียก `cache_clear()` เมื่อมุมเอียงเปลี่ยน — ดูตัวอย่างที่
  `settings_service.apply_effective_settings` ทำไว้แล้ว ถ้าลืมจะได้ค่าเก่าค้างทั้ง process
- ค่าคงที่ของโมเดล (ความสูงถัง ระยะห่าง ฯลฯ) ควรเข้า `settings_registry.py`
  เป็นกลุ่มใหม่ เพิ่ม 1 entry = ได้ช่องกรอกในหน้า Settings อัตโนมัติ ไม่ต้องทำ UI เอง

### 3. แสดงผล

- 3D View: ระบายสีแผงตามเงาที่ได้รับรายปี (มีระบบระบายสีแผงตาม output อยู่แล้ว ต่อยอดได้)
- หน้า Energy Report / Forecast: บอกว่า loss จากเงาเปลี่ยนไปเท่าไรจากค่าเดิม

## กฎเหล็กของ repo นี้ (ห้ามพลาด)

1. **ห้ามนำเสนอค่าที่แต่งขึ้นว่าเป็นของจริง** — ทุกค่าต้องระบุที่มา (`origin`)
   ว่าเป็นค่ายืนยันแล้ว / as-built / ค่าอ้างอิงงานวิจัย / ค่าประมาณ
   ถ้าไม่มีข้อมูลให้แสดง "ไม่มีข้อมูล" อย่างซื่อสัตย์ ห้ามเติมเลขให้กราฟดูสวย
2. **ไซต์นี้ไม่มีมิเตอร์วัดกำลังผลิตจริง** — เอาผลเงามาเทียบกับการผลิตจริงไม่ได้
   ต้องเขียนข้อจำกัดนี้บนหน้าจอ
3. `npm run test` **ไม่ typecheck** — ต้องรัน `npm run build` (`tsc -b`) ก่อน push เสมอ
4. **ห้าม annotate พารามิเตอร์ของ recharts formatter** — narrow ด้วย `typeof` ในบอดี้
   (พลาดมาแล้ว 3 ครั้ง ทำ CI แดง)
5. ต้องรัน `ruff check` เองก่อน push (sandbox ไม่รันให้)
6. รัน pytest **ทีละ package** (basename ซ้ำข้าม package ทำให้ collect error)
7. Handoff Report ต้องเป็นภาษาไทย 3 หัวข้อ + แท็ก Track และ **append** ลง `HANDOFF.md`

## ห้ามแตะ

- `financial/` — อีกบัญชีจะทำ P50/P90 + Monte Carlo (งาน B)
- `grid_carbon.py` / `green_savings.py` — เพิ่งแก้เสร็จ

## เริ่มยังไงดี (แนะนำลำดับ)

1. เปิด `config/assets.yaml` ดูก่อนว่าเรขาคณิตสิ่งกีดขวางมีอะไรบ้าง **แล้วรายงาน
   user ว่าขาดอะไร** ก่อนลงมือเขียนโค้ด — ถ้าข้อมูลไม่พอ ทั้งโปรเจกต์จะเป็นการเดา
2. ทำ ray-trace แบบง่ายสุดก่อน (จุดกึ่งกลางโซน vs สิ่งกีดขวางทรงกล่อง/ทรงกระบอก)
   ให้เห็นตัวเลขจริงก่อน ค่อยเพิ่มความละเอียดเป็นรายแผง
3. เทียบกับ `annual_shading_loss_pct` ค่าเดิม — **ถ้าต่างกันมาก ให้บอก user ก่อน
   เปลี่ยนค่า default** เพราะมันขยับ payback ที่เว็บเผยแพร่อยู่
4. เตือน user เรื่องเครดิตก่อนเริ่ม ถ้าประเมินว่างานยาว (นโยบายใน CLAUDE.md)

## ข้อมูลที่อาจต้องขอจาก user

- ความสูงจริงของถัง LNG และอาคารรอบๆ (ถ้า `assets.yaml` ยังไม่มี)
- ผังตำแหน่งแผงที่ละเอียดกว่าระดับโซน ถ้าจะทำ heatmap รายแผง
